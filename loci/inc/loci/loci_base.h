/* Copyright (c) 2008 The Board of Trustees of The Leland Stanford Junior University */
/* Copyright (c) 2011, 2012 Open Networking Foundation */
/* Copyright (c) 2012, 2013 Big Switch Networks, Inc. */
/* See the file LICENSE.loci which should have been included in the source distribution */

/****************************************************************
 * File: loci_base.h
 *
 * DO NOT EDIT
 *
 * This file is automatically generated
 *
 ****************************************************************/


#if !defined(_LOCI_BASE_H_)
#define _LOCI_BASE_H_


/*
 * Base OpenFlow definitions.  These depend only on standard C headers
 */
#include <string.h>
#include <stdint.h>

/* g++ requires this to pick up PRI, etc.
 * See  http://gcc.gnu.org/ml/gcc-help/2006-10/msg00223.html
 */
#if !defined(__STDC_FORMAT_MACROS)
#define __STDC_FORMAT_MACROS
#endif
#include <inttypes.h>

#include <stdlib.h>
#include <assert.h>
#include <loci/loci_idents.h>

/**
 * Macro to enable debugging for LOCI.
 *
 * This enables debug output to stdout.
 */
#define OF_DEBUG_ENABLE

#if defined(OF_DEBUG_ENABLE)
#include <stdio.h> /* Currently for debugging */
#define FIXME(str) do {                 \
        fprintf(stderr, "%s\n", str);  \
        exit(1);                        \
    } while (0)
#define debug printf
#else
#define FIXME(str)
#define debug(str, ...)
#endif /* OF_DEBUG_ENABLE */

/**
 * The type of a function used by the LOCI dump/show functions to
 * output text. Essentially the same signature as fprintf. May
 * be called many times per invocation of e.g. of_object_show().
 */
typedef int (*loci_writer_f)(void *cookie, const char *fmt, ...);

/**
 * Check if a version is supported
 */
#define OF_VERSION_OKAY(v) ((v) >= OF_VERSION_1_0 && (v) <= OF_VERSION_1_4)


/**
 * Enumeration of OpenFlow versions
 *
 * The wire protocol numbers are currently used for values of the corresponding
 * version identifiers.
 */
typedef enum of_version_e {
    OF_VERSION_UNKNOWN = 0,
    OF_VERSION_1_0 = 1,
    OF_VERSION_1_1 = 2,
    OF_VERSION_1_2 = 3,
    OF_VERSION_1_3 = 4,
    OF_VERSION_1_4 = 5
} of_version_t;

/**
 * @brief Use this when declaring arrays indexed by wire version
 */
#define OF_VERSION_ARRAY_MAX 6


typedef enum of_error_codes_e {
    OF_ERROR_NONE        = 0,
    OF_ERROR_RESOURCE    = -1,    /* Could not allocate space */
    OF_ERROR_PARAM       = -2,    /* Bad parameter */
    OF_ERROR_VERSION     = -3,    /* Version not supported */
    OF_ERROR_RANGE       = -4,    /* End of list indication */
    OF_ERROR_COMPAT      = -5,    /* Incompatible assignment */
    OF_ERROR_PARSE       = -6,    /* Error in parsing data */
    OF_ERROR_INIT        = -7,    /* Uninitialized data */
    OF_ERROR_UNKNOWN     = -8     /* Unknown error */
} of_error_codes_t;

#define OF_ERROR_STRINGS "none", \
    "resource", \
    "parameter", \
    "version", \
    "range", \
    "incompatible", \
    "parse", \
    "init", \
    "unknown"

extern const char *const of_error_strings[];

#ifdef __GNUC__
#define LOCI_NORETURN_ATTR __attribute__((__noreturn__))
#else
#define LOCI_NORETURN_ATTR
#endif

extern void loci_assert_fail(
    const char *cond,
    const char *file,
    unsigned int line) LOCI_NORETURN_ATTR;

#ifndef NDEBUG
#define LOCI_ASSERT(val) ((val) ? (void)0 : loci_assert_fail(#val, __FILE__, __LINE__))
#else
#define LOCI_ASSERT(val)
#endif

/*
 * Some LOCI object accessors can fail, and it's easy to forget to check.
 * On certain compilers we can trigger a warning if the error code
 * is ignored.
 */
#ifndef DISABLE_WARN_UNUSED_RESULT
#ifdef __GNUC__
#define WARN_UNUSED_RESULT __attribute__ ((warn_unused_result))
#else
#define WARN_UNUSED_RESULT
#endif
#else
#define WARN_UNUSED_RESULT
#endif

typedef union of_generic_u of_generic_t;
typedef struct of_object_s of_object_t;

/* Define ipv4 address as uint32 */
typedef uint32_t of_ipv4_t;

/* Table ID is the OF standard uint8 */
typedef uint8_t of_table_id_t;

#define OF_MAC_ADDR_BYTES 6
typedef struct of_mac_addr_s {
   uint8_t addr[OF_MAC_ADDR_BYTES];
} of_mac_addr_t;

#define OF_IPV6_BYTES 16
typedef struct of_ipv6_s {
   uint8_t addr[OF_IPV6_BYTES];
} of_ipv6_t;

/* Optical extension */
typedef struct of_odu_sig_id_s {
    uint16_t tpn;
    uint16_t length;
    uint8_t *tsmap;
} of_odu_sig_id_t;

typedef struct of_circuit_sig_id_s {
    uint8_t grid_type;
    uint8_t channel_spacing;
    uint16_t channel_number;
    uint16_t spectral_width;
} of_circuit_sig_id_t;

typedef struct of_och_sig_id_s {
    uint8_t grid_type;
    //enum ofp_channel_spacing channel_spacing;
    uint8_t channel_spacing;
    uint16_t n;
    uint16_t m;
} of_och_sig_id_t;

typedef struct of_bitmap_128_s {
    uint64_t hi;
    uint64_t lo;
} of_bitmap_128_t;

typedef struct of_checksum_128_s {
    uint64_t hi;
    uint64_t lo;
} of_checksum_128_t;

typedef struct of_bitmap_256_s {
    uint64_t words[4];
} of_bitmap_256_t;

typedef struct of_bitmap_512_s {
    uint64_t words[8];
} of_bitmap_512_t;

typedef struct of_ieee80211_mcs_rx_mask_s {
    uint8_t bytes[10];
} of_ieee80211_mcs_rx_mask_t;

extern const of_mac_addr_t of_mac_addr_all_ones;
extern const of_mac_addr_t of_mac_addr_all_zeros;

extern const of_ipv6_t of_ipv6_all_ones;
extern const of_ipv6_t of_ipv6_all_zeros;

extern const of_bitmap_256_t of_bitmap_256_all_ones;
extern const of_bitmap_256_t of_bitmap_256_all_zeroes;
extern const of_bitmap_512_t of_bitmap_512_all_ones;
extern const of_bitmap_512_t of_bitmap_512_all_zeroes;

/**
 * Generic zero and all-ones values of size 16 bytes.
 *
 * bitmap_512 is longest data type we worry about for comparisons
 */
#define of_all_zero_value of_bitmap_512_all_zeroes
#define of_all_ones_value of_bitmap_512_all_ones

/**
 * Non-zero/all ones check for arbitrary type of size <= 16 bytes
 */
#define OF_VARIABLE_IS_NON_ZERO(_ptr) \
    (MEMCMP(&of_all_zero_value, (_ptr), sizeof(*(_ptr))))
#define OF_VARIABLE_IS_ALL_ONES(_ptr) \
    (!MEMCMP(&of_all_ones_value, (_ptr), sizeof(*(_ptr))))

/* The octets object is a struct holding pointer and length */
typedef struct of_octets_s {
    uint8_t *data;
    int bytes;
} of_octets_t;

/* Macro to convert an octet object to a pointer; currently trivial */
#define OF_OCTETS_POINTER_GET(octet_ptr) ((octet_ptr)->data)
#define OF_OCTETS_POINTER_SET(octet_ptr, ptr) (octet_ptr)->data = (ptr)
#define OF_OCTETS_BYTES_GET(octet_ptr) ((octet_ptr)->bytes)
#define OF_OCTETS_BYTES_SET(octet_ptr, bytes) (octet_ptr)->bytes = (bytes)

/* Currently these are categorized as scalars */
typedef char of_port_name_t[OF_MAX_PORT_NAME_LEN];
typedef char of_app_code_t[OF_APP_CODE_LEN];
typedef char of_table_name_t[OF_MAX_TABLE_NAME_LEN];
typedef char of_desc_str_t[OF_DESC_STR_LEN];
typedef char of_serial_num_t[OF_SERIAL_NUM_LEN];
typedef char of_str6_t[6];
typedef char of_str32_t[32];
typedef char of_str64_t[64];

/* These are types which change across versions.  */
typedef uint32_t of_port_no_t;
typedef uint16_t of_fm_cmd_t;
typedef uint64_t of_wc_bmap_t;
typedef uint64_t of_match_bmap_t;

#define MEMMOVE(dest, src, bytes) memmove(dest, src, bytes)
#define MEMSET(dest, val, bytes) memset(dest, val, bytes)
#define MEMCPY(dest, src, bytes) memcpy(dest, src, bytes)
#define MEMCMP(a, b, bytes) memcmp(a, b, bytes)
#define MALLOC(bytes) malloc(bytes)
#define FREE(ptr) free(ptr)

/** Try an operation and return on failure. */
#define OF_TRY(op) do {                                                      \
        int _rv;                                                             \
        if ((_rv = (op)) < 0) {                                              \
            LOCI_LOG_ERROR("ERROR %d at %s:%d\n", _rv, __FILE__, __LINE__); \
            return _rv;                                                      \
        }                                                                    \
    } while (0)

/* The extent of an OF match object is determined by its length field, but
 * aligned to 8 bytes
 */

#define OF_MATCH_BYTES(length) (((length) + 7) & 0xfff8)

#if __BYTE_ORDER__ == __ORDER_BIG_ENDIAN__
#define U16_NTOH(val) (val)
#define U32_NTOH(val) (val)
#define U64_NTOH(val) (val)
#define IPV6_NTOH(dst, src) /* NOTE different syntax; currently no-op */
#define U16_HTON(val) (val)
#define U32_HTON(val) (val)
#define U64_HTON(val) (val)
#define IPV6_HTON(dst, src) /* NOTE different syntax; currently no-op */
#else /* Little Endian */
#define U16_NTOH(val) (((val) >> 8) | (((val) & 0xff) << 8))
#define U32_NTOH(val) ((((val) & 0xff000000) >> 24) |                   \
                       (((val) & 0x00ff0000) >>  8) |                   \
                       (((val) & 0x0000ff00) <<  8) |                   \
                       (((val) & 0x000000ff) << 24))
#define U64_NTOH(val) ((((val) & 0xff00000000000000LL) >> 56) |         \
                       (((val) & 0x00ff000000000000LL) >> 40) |         \
                       (((val) & 0x0000ff0000000000LL) >> 24) |         \
                       (((val) & 0x000000ff00000000LL) >>  8) |         \
                       (((val) & 0x00000000ff000000LL) <<  8) |         \
                       (((val) & 0x0000000000ff0000LL) << 24) |         \
                       (((val) & 0x000000000000ff00LL) << 40) |         \
                       (((val) & 0x00000000000000ffLL) << 56))
#define IPV6_NTOH(dst, src) /* NOTE different syntax; currently no-op */
#define U16_HTON(val) U16_NTOH(val)
#define U32_HTON(val) U32_NTOH(val)
#define U64_HTON(val) U64_NTOH(val)
#define IPV6_HTON(dst, src) /* NOTE different syntax; currently no-op */
#endif


/**
 * Enumeration of OpenFlow objects
 *
 * We enumerate the OpenFlow objects used internally.  Note that some
 * message types are determined both by an outer type (message type like
 * stats_request) and an inner type (port stats).  These are different
 * messages in ofC.
 *
 * These values are for internal use only.  They will change with
 * different versions of ofC.
 */

typedef enum of_object_id_e {
    /* Root object type */
    OF_OBJECT_INVALID = -1, /* "invalid" return value for mappings */
    OF_OBJECT = 0, /* Generic, untyped object */

    /* OpenFlow message objects */
    OF_AGGREGATE_STATS_REPLY = 1,
    OF_AGGREGATE_STATS_REQUEST = 2,
    OF_ASYNC_CONFIG_FAILED_ERROR_MSG = 3,
    OF_ASYNC_GET_REPLY = 4,
    OF_ASYNC_GET_REQUEST = 5,
    OF_ASYNC_SET = 6,
    OF_BAD_ACTION_ERROR_MSG = 7,
    OF_BAD_INSTRUCTION_ERROR_MSG = 8,
    OF_BAD_MATCH_ERROR_MSG = 9,
    OF_BAD_PROPERTY_ERROR_MSG = 10,
    OF_BAD_REQUEST_ERROR_MSG = 11,
    OF_BARRIER_REPLY = 12,
    OF_BARRIER_REQUEST = 13,
    OF_BSN_ARP_IDLE = 14,
    OF_BSN_BASE_ERROR = 15,
    OF_BSN_BW_CLEAR_DATA_REPLY = 16,
    OF_BSN_BW_CLEAR_DATA_REQUEST = 17,
    OF_BSN_BW_ENABLE_GET_REPLY = 18,
    OF_BSN_BW_ENABLE_GET_REQUEST = 19,
    OF_BSN_BW_ENABLE_SET_REPLY = 20,
    OF_BSN_BW_ENABLE_SET_REQUEST = 21,
    OF_BSN_CONTROLLER_CONNECTIONS_REPLY = 22,
    OF_BSN_CONTROLLER_CONNECTIONS_REQUEST = 23,
    OF_BSN_DEBUG_COUNTER_DESC_STATS_REPLY = 24,
    OF_BSN_DEBUG_COUNTER_DESC_STATS_REQUEST = 25,
    OF_BSN_DEBUG_COUNTER_STATS_REPLY = 26,
    OF_BSN_DEBUG_COUNTER_STATS_REQUEST = 27,
    OF_BSN_ERROR = 28,
    OF_BSN_FLOW_CHECKSUM_BUCKET_STATS_REPLY = 29,
    OF_BSN_FLOW_CHECKSUM_BUCKET_STATS_REQUEST = 30,
    OF_BSN_FLOW_IDLE = 31,
    OF_BSN_FLOW_IDLE_ENABLE_GET_REPLY = 32,
    OF_BSN_FLOW_IDLE_ENABLE_GET_REQUEST = 33,
    OF_BSN_FLOW_IDLE_ENABLE_SET_REPLY = 34,
    OF_BSN_FLOW_IDLE_ENABLE_SET_REQUEST = 35,
    OF_BSN_GENERIC_ASYNC = 36,
    OF_BSN_GENERIC_COMMAND = 37,
    OF_BSN_GENERIC_STATS_REPLY = 38,
    OF_BSN_GENERIC_STATS_REQUEST = 39,
    OF_BSN_GENTABLE_BUCKET_STATS_REPLY = 40,
    OF_BSN_GENTABLE_BUCKET_STATS_REQUEST = 41,
    OF_BSN_GENTABLE_CLEAR_REPLY = 42,
    OF_BSN_GENTABLE_CLEAR_REQUEST = 43,
    OF_BSN_GENTABLE_DESC_STATS_REPLY = 44,
    OF_BSN_GENTABLE_DESC_STATS_REQUEST = 45,
    OF_BSN_GENTABLE_ENTRY_ADD = 46,
    OF_BSN_GENTABLE_ENTRY_DELETE = 47,
    OF_BSN_GENTABLE_ENTRY_DESC_STATS_REPLY = 48,
    OF_BSN_GENTABLE_ENTRY_DESC_STATS_REQUEST = 49,
    OF_BSN_GENTABLE_ENTRY_STATS_REPLY = 50,
    OF_BSN_GENTABLE_ENTRY_STATS_REQUEST = 51,
    OF_BSN_GENTABLE_SET_BUCKETS_SIZE = 52,
    OF_BSN_GENTABLE_STATS_REPLY = 53,
    OF_BSN_GENTABLE_STATS_REQUEST = 54,
    OF_BSN_GET_INTERFACES_REPLY = 55,
    OF_BSN_GET_INTERFACES_REQUEST = 56,
    OF_BSN_GET_IP_MASK_REPLY = 57,
    OF_BSN_GET_IP_MASK_REQUEST = 58,
    OF_BSN_GET_L2_TABLE_REPLY = 59,
    OF_BSN_GET_L2_TABLE_REQUEST = 60,
    OF_BSN_GET_MIRRORING_REPLY = 61,
    OF_BSN_GET_MIRRORING_REQUEST = 62,
    OF_BSN_GET_SWITCH_PIPELINE_REPLY = 63,
    OF_BSN_GET_SWITCH_PIPELINE_REQUEST = 64,
    OF_BSN_HEADER = 65,
    OF_BSN_HYBRID_GET_REPLY = 66,
    OF_BSN_HYBRID_GET_REQUEST = 67,
    OF_BSN_IMAGE_DESC_STATS_REPLY = 68,
    OF_BSN_IMAGE_DESC_STATS_REQUEST = 69,
    OF_BSN_LACP_CONVERGENCE_NOTIF = 70,
    OF_BSN_LACP_STATS_REPLY = 71,
    OF_BSN_LACP_STATS_REQUEST = 72,
    OF_BSN_LOG = 73,
    OF_BSN_LUA_COMMAND_REPLY = 74,
    OF_BSN_LUA_COMMAND_REQUEST = 75,
    OF_BSN_LUA_NOTIFICATION = 76,
    OF_BSN_LUA_UPLOAD = 77,
    OF_BSN_PDU_RX_REPLY = 78,
    OF_BSN_PDU_RX_REQUEST = 79,
    OF_BSN_PDU_RX_TIMEOUT = 80,
    OF_BSN_PDU_TX_REPLY = 81,
    OF_BSN_PDU_TX_REQUEST = 82,
    OF_BSN_PORT_COUNTER_STATS_REPLY = 83,
    OF_BSN_PORT_COUNTER_STATS_REQUEST = 84,
    OF_BSN_ROLE_STATUS = 85,
    OF_BSN_SET_AUX_CXNS_REPLY = 86,
    OF_BSN_SET_AUX_CXNS_REQUEST = 87,
    OF_BSN_SET_IP_MASK = 88,
    OF_BSN_SET_L2_TABLE_REPLY = 89,
    OF_BSN_SET_L2_TABLE_REQUEST = 90,
    OF_BSN_SET_LACP_REPLY = 91,
    OF_BSN_SET_LACP_REQUEST = 92,
    OF_BSN_SET_MIRRORING = 93,
    OF_BSN_SET_PKTIN_SUPPRESSION_REPLY = 94,
    OF_BSN_SET_PKTIN_SUPPRESSION_REQUEST = 95,
    OF_BSN_SET_SWITCH_PIPELINE_REPLY = 96,
    OF_BSN_SET_SWITCH_PIPELINE_REQUEST = 97,
    OF_BSN_SHELL_COMMAND = 98,
    OF_BSN_SHELL_OUTPUT = 99,
    OF_BSN_SHELL_STATUS = 100,
    OF_BSN_STATS_REPLY = 101,
    OF_BSN_STATS_REQUEST = 102,
    OF_BSN_SWITCH_PIPELINE_STATS_REPLY = 103,
    OF_BSN_SWITCH_PIPELINE_STATS_REQUEST = 104,
    OF_BSN_TABLE_CHECKSUM_STATS_REPLY = 105,
    OF_BSN_TABLE_CHECKSUM_STATS_REQUEST = 106,
    OF_BSN_TABLE_SET_BUCKETS_SIZE = 107,
    OF_BSN_TAKEOVER = 108,
    OF_BSN_TIME_REPLY = 109,
    OF_BSN_TIME_REQUEST = 110,
    OF_BSN_VIRTUAL_PORT_CREATE_REPLY = 111,
    OF_BSN_VIRTUAL_PORT_CREATE_REQUEST = 112,
    OF_BSN_VIRTUAL_PORT_REMOVE_REPLY = 113,
    OF_BSN_VIRTUAL_PORT_REMOVE_REQUEST = 114,
    OF_BSN_VLAN_COUNTER_CLEAR = 115,
    OF_BSN_VLAN_COUNTER_STATS_REPLY = 116,
    OF_BSN_VLAN_COUNTER_STATS_REQUEST = 117,
    OF_BSN_VRF_COUNTER_STATS_REPLY = 118,
    OF_BSN_VRF_COUNTER_STATS_REQUEST = 119,
    OF_BUNDLE_ADD_MSG = 120,
    OF_BUNDLE_CTRL_MSG = 121,
    OF_BUNDLE_FAILED_ERROR_MSG = 122,
    OF_CALIENT_FLOW_STATS_REPLY = 123,
    OF_CALIENT_FLOW_STATS_REQUEST = 124,
    OF_CALIENT_PORT_DESC_STATS_REPLY = 125,
    OF_CALIENT_PORT_DESC_STATS_REQUEST = 126,
    OF_CALIENT_PORT_STATS_REPLY = 127,
    OF_CALIENT_PORT_STATS_REQUEST = 128,
    OF_CALIENT_PORT_STATUS = 129,
    OF_CALIENT_STATS_REPLY = 130,
    OF_CALIENT_STATS_REQUEST = 131,
    OF_CIRCUIT_PORT_STATUS = 132,
    OF_CIRCUIT_PORTS_REPLY = 133,
    OF_CIRCUIT_PORTS_REQUEST = 134,
    OF_DESC_STATS_REPLY = 135,
    OF_DESC_STATS_REQUEST = 136,
    OF_ECHO_REPLY = 137,
    OF_ECHO_REQUEST = 138,
    OF_ERROR_MSG = 139,
    OF_EXP_OPTICAL_EXT_REPLY = 140,
    OF_EXP_OPTICAL_EXT_REQUEST = 141,
    OF_EXP_PORT_ADJACENCY_REPLY = 142,
    OF_EXP_PORT_ADJACENCY_REQUEST = 143,
    OF_EXP_PORT_DESC_REPLY = 144,
    OF_EXP_PORT_DESC_REQUEST = 145,
    OF_EXPERIMENTER = 146,
    OF_EXPERIMENTER_ERROR_MSG = 147,
    OF_EXPERIMENTER_STATS_REPLY = 148,
    OF_EXPERIMENTER_STATS_REQUEST = 149,
    OF_FEATURES_REPLY = 150,
    OF_FEATURES_REQUEST = 151,
    OF_FLOW_ADD = 152,
    OF_FLOW_DELETE = 153,
    OF_FLOW_DELETE_STRICT = 154,
    OF_FLOW_MOD = 155,
    OF_FLOW_MOD_FAILED_ERROR_MSG = 156,
    OF_FLOW_MODIFY = 157,
    OF_FLOW_MODIFY_STRICT = 158,
    OF_FLOW_MONITOR_FAILED_ERROR_MSG = 159,
    OF_FLOW_REMOVED = 160,
    OF_FLOW_STATS_REPLY = 161,
    OF_FLOW_STATS_REQUEST = 162,
    OF_GET_CONFIG_REPLY = 163,
    OF_GET_CONFIG_REQUEST = 164,
    OF_GROUP_ADD = 165,
    OF_GROUP_DELETE = 166,
    OF_GROUP_DESC_STATS_REPLY = 167,
    OF_GROUP_DESC_STATS_REQUEST = 168,
    OF_GROUP_FEATURES_STATS_REPLY = 169,
    OF_GROUP_FEATURES_STATS_REQUEST = 170,
    OF_GROUP_MOD = 171,
    OF_GROUP_MOD_FAILED_ERROR_MSG = 172,
    OF_GROUP_MODIFY = 173,
    OF_GROUP_STATS_REPLY = 174,
    OF_GROUP_STATS_REQUEST = 175,
    OF_HEADER = 176,
    OF_HELLO = 177,
    OF_HELLO_FAILED_ERROR_MSG = 178,
    OF_METER_CONFIG_STATS_REPLY = 179,
    OF_METER_CONFIG_STATS_REQUEST = 180,
    OF_METER_FEATURES_STATS_REPLY = 181,
    OF_METER_FEATURES_STATS_REQUEST = 182,
    OF_METER_MOD = 183,
    OF_METER_MOD_FAILED_ERROR_MSG = 184,
    OF_METER_STATS_REPLY = 185,
    OF_METER_STATS_REQUEST = 186,
    OF_NICIRA_CONTROLLER_ROLE_REPLY = 187,
    OF_NICIRA_CONTROLLER_ROLE_REQUEST = 188,
    OF_NICIRA_HEADER = 189,
    OF_OPLINK_CHANNEL_POWER_REPLY = 190,
    OF_OPLINK_CHANNEL_POWER_REQUEST = 191,
    OF_OPLINK_EXP_HEADER = 192,
    OF_OPLINK_PORT_POWER_REPLY = 193,
    OF_OPLINK_PORT_POWER_REQUEST = 194,
    OF_OPLINK_PORT_POWER_SET = 195,
    OF_OPLINK_STATS_REPLY = 196,
    OF_OPLINK_STATS_REQUEST = 197,
    OF_PACKET_IN = 198,
    OF_PACKET_OUT = 199,
    OF_PORT_DESC_STATS_REPLY = 200,
    OF_PORT_DESC_STATS_REQUEST = 201,
    OF_PORT_MOD = 202,
    OF_PORT_MOD_FAILED_ERROR_MSG = 203,
    OF_PORT_STATS_REPLY = 204,
    OF_PORT_STATS_REQUEST = 205,
    OF_PORT_STATUS = 206,
    OF_QUEUE_DESC_STATS_REPLY = 207,
    OF_QUEUE_DESC_STATS_REQUEST = 208,
    OF_QUEUE_GET_CONFIG_REPLY = 209,
    OF_QUEUE_GET_CONFIG_REQUEST = 210,
    OF_QUEUE_OP_FAILED_ERROR_MSG = 211,
    OF_QUEUE_STATS_REPLY = 212,
    OF_QUEUE_STATS_REQUEST = 213,
    OF_REQUESTFORWARD = 214,
    OF_ROLE_REPLY = 215,
    OF_ROLE_REQUEST = 216,
    OF_ROLE_REQUEST_FAILED_ERROR_MSG = 217,
    OF_ROLE_STATUS = 218,
    OF_SDWN_ADD_CLIENT = 219,
    OF_SDWN_ADD_LVAP = 220,
    OF_SDWN_DEL_CLIENT = 221,
    OF_SDWN_DEL_LVAP = 222,
    OF_SDWN_GET_CHANNEL_REPLY = 223,
    OF_SDWN_GET_CHANNEL_REQUEST = 224,
    OF_SDWN_GET_CLIENTS_REPLY = 225,
    OF_SDWN_GET_CLIENTS_REPLY_CRYPTO = 226,
    OF_SDWN_GET_CLIENTS_REPLY_LVAP = 227,
    OF_SDWN_GET_CLIENTS_REPLY_NORMAL = 228,
    OF_SDWN_GET_CLIENTS_REQUEST = 229,
    OF_SDWN_GET_REMOTE_PORT_REPLY = 230,
    OF_SDWN_GET_REMOTE_PORT_REQUEST = 231,
    OF_SDWN_HEADER = 232,
    OF_SDWN_IEEE80211_MGMT = 233,
    OF_SDWN_IEEE80211_MGMT_REPLY = 234,
    OF_SDWN_MOD_LVAP = 235,
    OF_SDWN_PACKET_IN = 236,
    OF_SDWN_PORT_DESC_REPLY = 237,
    OF_SDWN_PORT_DESC_REQUEST = 238,
    OF_SDWN_REPLY = 239,
    OF_SDWN_REQUEST = 240,
    OF_SDWN_SET_CHANNEL = 241,
    OF_SET_CONFIG = 242,
    OF_STATS_REPLY = 243,
    OF_STATS_REQUEST = 244,
    OF_SWITCH_CONFIG_FAILED_ERROR_MSG = 245,
    OF_TABLE_DESC_STATS_REPLY = 246,
    OF_TABLE_DESC_STATS_REQUEST = 247,
    OF_TABLE_FEATURES_FAILED_ERROR_MSG = 248,
    OF_TABLE_FEATURES_STATS_REPLY = 249,
    OF_TABLE_FEATURES_STATS_REQUEST = 250,
    OF_TABLE_MOD = 251,
    OF_TABLE_MOD_FAILED_ERROR_MSG = 252,
    OF_TABLE_STATS_REPLY = 253,
    OF_TABLE_STATS_REQUEST = 254,
    OF_TABLE_STATUS = 255,

    /* Non-message objects */
    OF_ACTION = 256,
    OF_ACTION_BSN = 257,
    OF_ACTION_BSN_CHECKSUM = 258,
    OF_ACTION_BSN_GENTABLE = 259,
    OF_ACTION_BSN_MIRROR = 260,
    OF_ACTION_BSN_SET_TUNNEL_DST = 261,
    OF_ACTION_CIRCUIT = 262,
    OF_ACTION_COPY_TTL_IN = 263,
    OF_ACTION_COPY_TTL_OUT = 264,
    OF_ACTION_DEC_MPLS_TTL = 265,
    OF_ACTION_DEC_NW_TTL = 266,
    OF_ACTION_ENQUEUE = 267,
    OF_ACTION_EXPERIMENTER = 268,
    OF_ACTION_GROUP = 269,
    OF_ACTION_ID = 270,
    OF_ACTION_ID_BSN = 271,
    OF_ACTION_ID_BSN_CHECKSUM = 272,
    OF_ACTION_ID_BSN_GENTABLE = 273,
    OF_ACTION_ID_BSN_MIRROR = 274,
    OF_ACTION_ID_BSN_SET_TUNNEL_DST = 275,
    OF_ACTION_ID_CIRCUIT = 276,
    OF_ACTION_ID_COPY_TTL_IN = 277,
    OF_ACTION_ID_COPY_TTL_OUT = 278,
    OF_ACTION_ID_DEC_MPLS_TTL = 279,
    OF_ACTION_ID_DEC_NW_TTL = 280,
    OF_ACTION_ID_EXPERIMENTER = 281,
    OF_ACTION_ID_GROUP = 282,
    OF_ACTION_ID_NICIRA = 283,
    OF_ACTION_ID_NICIRA_DEC_TTL = 284,
    OF_ACTION_ID_NICIRA_MOVE = 285,
    OF_ACTION_ID_NICIRA_POP_NSH = 286,
    OF_ACTION_ID_NICIRA_PUSH_NSH = 287,
    OF_ACTION_ID_NICIRA_RESUBMIT = 288,
    OF_ACTION_ID_NICIRA_RESUBMIT_TABLE = 289,
    OF_ACTION_ID_OFDPA = 290,
    OF_ACTION_ID_OFDPA_POP_CW = 291,
    OF_ACTION_ID_OFDPA_POP_L2_HEADER = 292,
    OF_ACTION_ID_OFDPA_PUSH_CW = 293,
    OF_ACTION_ID_OFDPA_PUSH_L2_HEADER = 294,
    OF_ACTION_ID_OPLINK_ATT = 295,
    OF_ACTION_ID_OUTPUT = 296,
    OF_ACTION_ID_POP_MPLS = 297,
    OF_ACTION_ID_POP_PBB = 298,
    OF_ACTION_ID_POP_VLAN = 299,
    OF_ACTION_ID_PUSH_MPLS = 300,
    OF_ACTION_ID_PUSH_PBB = 301,
    OF_ACTION_ID_PUSH_VLAN = 302,
    OF_ACTION_ID_SET_FIELD = 303,
    OF_ACTION_ID_SET_MPLS_TTL = 304,
    OF_ACTION_ID_SET_NW_TTL = 305,
    OF_ACTION_ID_SET_QUEUE = 306,
    OF_ACTION_NICIRA = 307,
    OF_ACTION_NICIRA_DEC_TTL = 308,
    OF_ACTION_NICIRA_MOVE = 309,
    OF_ACTION_NICIRA_POP_NSH = 310,
    OF_ACTION_NICIRA_PUSH_NSH = 311,
    OF_ACTION_NICIRA_RESUBMIT = 312,
    OF_ACTION_NICIRA_RESUBMIT_TABLE = 313,
    OF_ACTION_OFDPA = 314,
    OF_ACTION_OFDPA_POP_CW = 315,
    OF_ACTION_OFDPA_POP_L2_HEADER = 316,
    OF_ACTION_OFDPA_PUSH_CW = 317,
    OF_ACTION_OFDPA_PUSH_L2_HEADER = 318,
    OF_ACTION_OPLINK_ATT = 319,
    OF_ACTION_OUTPUT = 320,
    OF_ACTION_POP_MPLS = 321,
    OF_ACTION_POP_PBB = 322,
    OF_ACTION_POP_VLAN = 323,
    OF_ACTION_PUSH_MPLS = 324,
    OF_ACTION_PUSH_PBB = 325,
    OF_ACTION_PUSH_VLAN = 326,
    OF_ACTION_SET_DL_DST = 327,
    OF_ACTION_SET_DL_SRC = 328,
    OF_ACTION_SET_FIELD = 329,
    OF_ACTION_SET_MPLS_LABEL = 330,
    OF_ACTION_SET_MPLS_TC = 331,
    OF_ACTION_SET_MPLS_TTL = 332,
    OF_ACTION_SET_NW_DST = 333,
    OF_ACTION_SET_NW_ECN = 334,
    OF_ACTION_SET_NW_SRC = 335,
    OF_ACTION_SET_NW_TOS = 336,
    OF_ACTION_SET_NW_TTL = 337,
    OF_ACTION_SET_QUEUE = 338,
    OF_ACTION_SET_TP_DST = 339,
    OF_ACTION_SET_TP_SRC = 340,
    OF_ACTION_SET_VLAN_PCP = 341,
    OF_ACTION_SET_VLAN_VID = 342,
    OF_ACTION_STRIP_VLAN = 343,
    OF_ASYNC_CONFIG_PROP = 344,
    OF_ASYNC_CONFIG_PROP_EXPERIMENTER_MASTER = 345,
    OF_ASYNC_CONFIG_PROP_EXPERIMENTER_SLAVE = 346,
    OF_ASYNC_CONFIG_PROP_FLOW_REMOVED_MASTER = 347,
    OF_ASYNC_CONFIG_PROP_FLOW_REMOVED_SLAVE = 348,
    OF_ASYNC_CONFIG_PROP_PACKET_IN_MASTER = 349,
    OF_ASYNC_CONFIG_PROP_PACKET_IN_SLAVE = 350,
    OF_ASYNC_CONFIG_PROP_PORT_STATUS_MASTER = 351,
    OF_ASYNC_CONFIG_PROP_PORT_STATUS_SLAVE = 352,
    OF_ASYNC_CONFIG_PROP_REQUESTFORWARD_MASTER = 353,
    OF_ASYNC_CONFIG_PROP_REQUESTFORWARD_SLAVE = 354,
    OF_ASYNC_CONFIG_PROP_ROLE_STATUS_MASTER = 355,
    OF_ASYNC_CONFIG_PROP_ROLE_STATUS_SLAVE = 356,
    OF_ASYNC_CONFIG_PROP_TABLE_STATUS_MASTER = 357,
    OF_ASYNC_CONFIG_PROP_TABLE_STATUS_SLAVE = 358,
    OF_BSN_CONTROLLER_CONNECTION = 359,
    OF_BSN_DEBUG_COUNTER_DESC_STATS_ENTRY = 360,
    OF_BSN_DEBUG_COUNTER_STATS_ENTRY = 361,
    OF_BSN_FLOW_CHECKSUM_BUCKET_STATS_ENTRY = 362,
    OF_BSN_GENERIC_STATS_ENTRY = 363,
    OF_BSN_GENTABLE_BUCKET_STATS_ENTRY = 364,
    OF_BSN_GENTABLE_DESC_STATS_ENTRY = 365,
    OF_BSN_GENTABLE_ENTRY_DESC_STATS_ENTRY = 366,
    OF_BSN_GENTABLE_ENTRY_STATS_ENTRY = 367,
    OF_BSN_GENTABLE_STATS_ENTRY = 368,
    OF_BSN_INTERFACE = 369,
    OF_BSN_LACP_STATS_ENTRY = 370,
    OF_BSN_PORT_COUNTER_STATS_ENTRY = 371,
    OF_BSN_SWITCH_PIPELINE_STATS_ENTRY = 372,
    OF_BSN_TABLE_CHECKSUM_STATS_ENTRY = 373,
    OF_BSN_TLV = 374,
    OF_BSN_TLV_ACTOR_KEY = 375,
    OF_BSN_TLV_ACTOR_PORT_NUM = 376,
    OF_BSN_TLV_ACTOR_PORT_PRIORITY = 377,
    OF_BSN_TLV_ACTOR_STATE = 378,
    OF_BSN_TLV_ACTOR_SYSTEM_MAC = 379,
    OF_BSN_TLV_ACTOR_SYSTEM_PRIORITY = 380,
    OF_BSN_TLV_ANCHOR = 381,
    OF_BSN_TLV_APPLY_BYTES = 382,
    OF_BSN_TLV_APPLY_PACKETS = 383,
    OF_BSN_TLV_AUTO_NEGOTIATION = 384,
    OF_BSN_TLV_BROADCAST_QUERY_TIMEOUT = 385,
    OF_BSN_TLV_BROADCAST_RATE = 386,
    OF_BSN_TLV_BUCKET = 387,
    OF_BSN_TLV_CIRCUIT_ID = 388,
    OF_BSN_TLV_CONVERGENCE_STATUS = 389,
    OF_BSN_TLV_CPU_LAG = 390,
    OF_BSN_TLV_CRC_ENABLED = 391,
    OF_BSN_TLV_DATA = 392,
    OF_BSN_TLV_DATA_MASK = 393,
    OF_BSN_TLV_DECAP = 394,
    OF_BSN_TLV_DISABLE_SRC_MAC_CHECK = 395,
    OF_BSN_TLV_DROP = 396,
    OF_BSN_TLV_DSCP = 397,
    OF_BSN_TLV_ECN = 398,
    OF_BSN_TLV_EGRESS_ONLY = 399,
    OF_BSN_TLV_EGRESS_PORT_GROUP_ID = 400,
    OF_BSN_TLV_ENHANCED_HASH_CAPABILITY = 401,
    OF_BSN_TLV_ETH_DST = 402,
    OF_BSN_TLV_ETH_SRC = 403,
    OF_BSN_TLV_ETH_TYPE = 404,
    OF_BSN_TLV_EXTERNAL_GATEWAY_IP = 405,
    OF_BSN_TLV_EXTERNAL_GATEWAY_MAC = 406,
    OF_BSN_TLV_EXTERNAL_IP = 407,
    OF_BSN_TLV_EXTERNAL_MAC = 408,
    OF_BSN_TLV_EXTERNAL_NETMASK = 409,
    OF_BSN_TLV_GENERATION_ID = 410,
    OF_BSN_TLV_HASH_GTP_HEADER_MATCH = 411,
    OF_BSN_TLV_HASH_GTP_PORT_MATCH = 412,
    OF_BSN_TLV_HASH_PACKET_FIELD = 413,
    OF_BSN_TLV_HASH_PACKET_TYPE = 414,
    OF_BSN_TLV_HASH_SEED = 415,
    OF_BSN_TLV_HASH_TYPE = 416,
    OF_BSN_TLV_HEADER_SIZE = 417,
    OF_BSN_TLV_ICMP_CODE = 418,
    OF_BSN_TLV_ICMP_ID = 419,
    OF_BSN_TLV_ICMP_TYPE = 420,
    OF_BSN_TLV_ICMPV6_CHKSUM = 421,
    OF_BSN_TLV_IDLE_NOTIFICATION = 422,
    OF_BSN_TLV_IDLE_TIME = 423,
    OF_BSN_TLV_IDLE_TIMEOUT = 424,
    OF_BSN_TLV_IGMP_SNOOPING = 425,
    OF_BSN_TLV_INGRESS_PORT_GROUP_ID = 426,
    OF_BSN_TLV_INTERNAL_GATEWAY_MAC = 427,
    OF_BSN_TLV_INTERNAL_MAC = 428,
    OF_BSN_TLV_INTERVAL = 429,
    OF_BSN_TLV_IP_PROTO = 430,
    OF_BSN_TLV_IPV4 = 431,
    OF_BSN_TLV_IPV4_DST = 432,
    OF_BSN_TLV_IPV4_NETMASK = 433,
    OF_BSN_TLV_IPV4_SRC = 434,
    OF_BSN_TLV_IPV6 = 435,
    OF_BSN_TLV_IPV6_DST = 436,
    OF_BSN_TLV_IPV6_PREFIX = 437,
    OF_BSN_TLV_IPV6_SRC = 438,
    OF_BSN_TLV_KNOWN_MULTICAST_RATE = 439,
    OF_BSN_TLV_L2_MULTICAST_LOOKUP = 440,
    OF_BSN_TLV_L3_DST_CLASS_ID = 441,
    OF_BSN_TLV_L3_INTERFACE_CLASS_ID = 442,
    OF_BSN_TLV_L3_SRC_CLASS_ID = 443,
    OF_BSN_TLV_LOOPBACK_PORT = 444,
    OF_BSN_TLV_MAC = 445,
    OF_BSN_TLV_MAC_MASK = 446,
    OF_BSN_TLV_MCG_TYPE_VXLAN = 447,
    OF_BSN_TLV_MISS_PACKETS = 448,
    OF_BSN_TLV_MPLS_CONTROL_WORD = 449,
    OF_BSN_TLV_MPLS_LABEL = 450,
    OF_BSN_TLV_MPLS_SEQUENCED = 451,
    OF_BSN_TLV_MULTICAST_INTERFACE_ID = 452,
    OF_BSN_TLV_NAME = 453,
    OF_BSN_TLV_NDP_OFFLOAD = 454,
    OF_BSN_TLV_NDP_STATIC = 455,
    OF_BSN_TLV_NEGATE = 456,
    OF_BSN_TLV_NEXT_HOP_IPV4 = 457,
    OF_BSN_TLV_NEXT_HOP_MAC = 458,
    OF_BSN_TLV_NEXTHOP_TYPE_VXLAN = 459,
    OF_BSN_TLV_OFFSET = 460,
    OF_BSN_TLV_PARENT_PORT = 461,
    OF_BSN_TLV_PARTNER_KEY = 462,
    OF_BSN_TLV_PARTNER_PORT_NUM = 463,
    OF_BSN_TLV_PARTNER_PORT_PRIORITY = 464,
    OF_BSN_TLV_PARTNER_STATE = 465,
    OF_BSN_TLV_PARTNER_SYSTEM_MAC = 466,
    OF_BSN_TLV_PARTNER_SYSTEM_PRIORITY = 467,
    OF_BSN_TLV_PORT = 468,
    OF_BSN_TLV_PORT_USAGE = 469,
    OF_BSN_TLV_PORT_VXLAN_MODE = 470,
    OF_BSN_TLV_PRIORITY = 471,
    OF_BSN_TLV_PUSH_VLAN_ON_INGRESS = 472,
    OF_BSN_TLV_QOS_PRIORITY = 473,
    OF_BSN_TLV_QUEUE_ID = 474,
    OF_BSN_TLV_QUEUE_WEIGHT = 475,
    OF_BSN_TLV_RATE_LIMIT = 476,
    OF_BSN_TLV_RATE_UNIT = 477,
    OF_BSN_TLV_REFERENCE = 478,
    OF_BSN_TLV_REPLY_PACKETS = 479,
    OF_BSN_TLV_REQUEST_PACKETS = 480,
    OF_BSN_TLV_RX_BYTES = 481,
    OF_BSN_TLV_RX_PACKETS = 482,
    OF_BSN_TLV_SAMPLING_RATE = 483,
    OF_BSN_TLV_SET_LOOPBACK_MODE = 484,
    OF_BSN_TLV_STATUS = 485,
    OF_BSN_TLV_STRIP_MPLS_L2_ON_INGRESS = 486,
    OF_BSN_TLV_STRIP_MPLS_L3_ON_INGRESS = 487,
    OF_BSN_TLV_STRIP_VLAN_ON_EGRESS = 488,
    OF_BSN_TLV_SUB_AGENT_ID = 489,
    OF_BSN_TLV_TCP_DST = 490,
    OF_BSN_TLV_TCP_FLAGS = 491,
    OF_BSN_TLV_TCP_SRC = 492,
    OF_BSN_TLV_TTL = 493,
    OF_BSN_TLV_TUNNEL_CAPABILITY = 494,
    OF_BSN_TLV_TX_BYTES = 495,
    OF_BSN_TLV_TX_PACKETS = 496,
    OF_BSN_TLV_UDF_ANCHOR = 497,
    OF_BSN_TLV_UDF_ID = 498,
    OF_BSN_TLV_UDF_LENGTH = 499,
    OF_BSN_TLV_UDF_OFFSET = 500,
    OF_BSN_TLV_UDP_DST = 501,
    OF_BSN_TLV_UDP_SRC = 502,
    OF_BSN_TLV_UINT64_LIST = 503,
    OF_BSN_TLV_UNICAST_QUERY_TIMEOUT = 504,
    OF_BSN_TLV_UNICAST_RATE = 505,
    OF_BSN_TLV_UNKNOWN_MULTICAST_RATE = 506,
    OF_BSN_TLV_UNTAGGED = 507,
    OF_BSN_TLV_USE_PACKET_STATE = 508,
    OF_BSN_TLV_VFI = 509,
    OF_BSN_TLV_VFP_CLASS_ID = 510,
    OF_BSN_TLV_VLAN_MAC_LIST = 511,
    OF_BSN_TLV_VLAN_PCP = 512,
    OF_BSN_TLV_VLAN_VID = 513,
    OF_BSN_TLV_VLAN_VID_MASK = 514,
    OF_BSN_TLV_VNI = 515,
    OF_BSN_TLV_VPN_KEY = 516,
    OF_BSN_TLV_VRF = 517,
    OF_BSN_TLV_VXLAN_EGRESS_LAG = 518,
    OF_BSN_VLAN_COUNTER_STATS_ENTRY = 519,
    OF_BSN_VLAN_MAC = 520,
    OF_BSN_VPORT = 521,
    OF_BSN_VPORT_L2GRE = 522,
    OF_BSN_VPORT_Q_IN_Q = 523,
    OF_BSN_VRF_COUNTER_STATS_ENTRY = 524,
    OF_BUCKET = 525,
    OF_BUCKET_COUNTER = 526,
    OF_BUNDLE_PROP = 527,
    OF_BUNDLE_PROP_EXPERIMENTER = 528,
    OF_CALIENT_FLOW_STATS_ENTRY = 529,
    OF_CALIENT_PORT_DESC_PROP = 530,
    OF_CALIENT_PORT_DESC_PROP_OPTICAL = 531,
    OF_CALIENT_PORT_DESC_STATS_ENTRY = 532,
    OF_CALIENT_PORT_STATS_ENTRY = 533,
    OF_EXP_EXT_AD_ID = 534,
    OF_EXP_EXT_AD_ID_PORT_OTN = 535,
    OF_EXP_EXT_AD_ID_SONET = 536,
    OF_EXP_PORT = 537,
    OF_EXP_PORT_ADJACENCY = 538,
    OF_EXP_PORT_ADJACENCY_ID = 539,
    OF_EXP_PORT_DES_PROP_HEADER = 540,
    OF_EXP_PORT_DESC_PROP_OPTICAL_TRANSPORT = 541,
    OF_EXP_PORT_OPTICAL_TRANSPORT_FEATURE_HEADER = 542,
    OF_EXP_PORT_OPTICAL_TRANSPORT_LAYER_ENTRY = 543,
    OF_EXP_PORT_OPTICAL_TRANSPORT_LAYER_STACK = 544,
    OF_FLOW_STATS_ENTRY = 545,
    OF_GROUP_DESC_STATS_ENTRY = 546,
    OF_GROUP_STATS_ENTRY = 547,
    OF_HELLO_ELEM = 548,
    OF_HELLO_ELEM_VERSIONBITMAP = 549,
    OF_IEEE80211_HT_CAP = 550,
    OF_IEEE80211_MCS_INFO = 551,
    OF_IEEE80211_VHT_CAP = 552,
    OF_IEEE80211_VHT_MCS_INFO = 553,
    OF_INSTRUCTION = 554,
    OF_INSTRUCTION_APPLY_ACTIONS = 555,
    OF_INSTRUCTION_BSN = 556,
    OF_INSTRUCTION_BSN_ARP_OFFLOAD = 557,
    OF_INSTRUCTION_BSN_AUTO_NEGOTIATION = 558,
    OF_INSTRUCTION_BSN_DENY = 559,
    OF_INSTRUCTION_BSN_DHCP_OFFLOAD = 560,
    OF_INSTRUCTION_BSN_DISABLE_L3 = 561,
    OF_INSTRUCTION_BSN_DISABLE_SPLIT_HORIZON_CHECK = 562,
    OF_INSTRUCTION_BSN_DISABLE_SRC_MAC_CHECK = 563,
    OF_INSTRUCTION_BSN_DISABLE_VLAN_COUNTERS = 564,
    OF_INSTRUCTION_BSN_HASH_SELECT = 565,
    OF_INSTRUCTION_BSN_INTERNAL_PRIORITY = 566,
    OF_INSTRUCTION_BSN_NDP_OFFLOAD = 567,
    OF_INSTRUCTION_BSN_PACKET_OF_DEATH = 568,
    OF_INSTRUCTION_BSN_PERMIT = 569,
    OF_INSTRUCTION_BSN_PRIORITIZE_PDUS = 570,
    OF_INSTRUCTION_BSN_REQUIRE_VLAN_XLATE = 571,
    OF_INSTRUCTION_BSN_SPAN_DESTINATION = 572,
    OF_INSTRUCTION_CLEAR_ACTIONS = 573,
    OF_INSTRUCTION_EXPERIMENTER = 574,
    OF_INSTRUCTION_GOTO_TABLE = 575,
    OF_INSTRUCTION_ID = 576,
    OF_INSTRUCTION_ID_APPLY_ACTIONS = 577,
    OF_INSTRUCTION_ID_BSN = 578,
    OF_INSTRUCTION_ID_BSN_ARP_OFFLOAD = 579,
    OF_INSTRUCTION_ID_BSN_AUTO_NEGOTIATION = 580,
    OF_INSTRUCTION_ID_BSN_DENY = 581,
    OF_INSTRUCTION_ID_BSN_DHCP_OFFLOAD = 582,
    OF_INSTRUCTION_ID_BSN_DISABLE_L3 = 583,
    OF_INSTRUCTION_ID_BSN_DISABLE_SPLIT_HORIZON_CHECK = 584,
    OF_INSTRUCTION_ID_BSN_DISABLE_SRC_MAC_CHECK = 585,
    OF_INSTRUCTION_ID_BSN_DISABLE_VLAN_COUNTERS = 586,
    OF_INSTRUCTION_ID_BSN_HASH_SELECT = 587,
    OF_INSTRUCTION_ID_BSN_INTERNAL_PRIORITY = 588,
    OF_INSTRUCTION_ID_BSN_NDP_OFFLOAD = 589,
    OF_INSTRUCTION_ID_BSN_PACKET_OF_DEATH = 590,
    OF_INSTRUCTION_ID_BSN_PERMIT = 591,
    OF_INSTRUCTION_ID_BSN_PRIORITIZE_PDUS = 592,
    OF_INSTRUCTION_ID_BSN_REQUIRE_VLAN_XLATE = 593,
    OF_INSTRUCTION_ID_BSN_SPAN_DESTINATION = 594,
    OF_INSTRUCTION_ID_CLEAR_ACTIONS = 595,
    OF_INSTRUCTION_ID_EXPERIMENTER = 596,
    OF_INSTRUCTION_ID_GOTO_TABLE = 597,
    OF_INSTRUCTION_ID_METER = 598,
    OF_INSTRUCTION_ID_WRITE_ACTIONS = 599,
    OF_INSTRUCTION_ID_WRITE_METADATA = 600,
    OF_INSTRUCTION_METER = 601,
    OF_INSTRUCTION_WRITE_ACTIONS = 602,
    OF_INSTRUCTION_WRITE_METADATA = 603,
    OF_MATCH_V1 = 604,
    OF_MATCH_V2 = 605,
    OF_MATCH_V3 = 606,
    OF_METER_BAND = 607,
    OF_METER_BAND_DROP = 608,
    OF_METER_BAND_DSCP_REMARK = 609,
    OF_METER_BAND_EXPERIMENTER = 610,
    OF_METER_BAND_STATS = 611,
    OF_METER_CONFIG = 612,
    OF_METER_FEATURES = 613,
    OF_METER_STATS = 614,
    OF_OPLINK_CHANNEL_POWER = 615,
    OF_OPLINK_PORT_POWER = 616,
    OF_OXM = 617,
    OF_OXM_ARP_OP = 618,
    OF_OXM_ARP_OP_MASKED = 619,
    OF_OXM_ARP_SHA = 620,
    OF_OXM_ARP_SHA_MASKED = 621,
    OF_OXM_ARP_SPA = 622,
    OF_OXM_ARP_SPA_MASKED = 623,
    OF_OXM_ARP_THA = 624,
    OF_OXM_ARP_THA_MASKED = 625,
    OF_OXM_ARP_TPA = 626,
    OF_OXM_ARP_TPA_MASKED = 627,
    OF_OXM_BSN_EGR_PORT_GROUP_ID = 628,
    OF_OXM_BSN_EGR_PORT_GROUP_ID_MASKED = 629,
    OF_OXM_BSN_GLOBAL_VRF_ALLOWED = 630,
    OF_OXM_BSN_GLOBAL_VRF_ALLOWED_MASKED = 631,
    OF_OXM_BSN_IN_PORTS_128 = 632,
    OF_OXM_BSN_IN_PORTS_128_MASKED = 633,
    OF_OXM_BSN_IN_PORTS_512 = 634,
    OF_OXM_BSN_IN_PORTS_512_MASKED = 635,
    OF_OXM_BSN_INGRESS_PORT_GROUP_ID = 636,
    OF_OXM_BSN_INGRESS_PORT_GROUP_ID_MASKED = 637,
    OF_OXM_BSN_INNER_ETH_DST = 638,
    OF_OXM_BSN_INNER_ETH_DST_MASKED = 639,
    OF_OXM_BSN_INNER_ETH_SRC = 640,
    OF_OXM_BSN_INNER_ETH_SRC_MASKED = 641,
    OF_OXM_BSN_INNER_VLAN_VID = 642,
    OF_OXM_BSN_INNER_VLAN_VID_MASKED = 643,
    OF_OXM_BSN_IP_FRAGMENTATION = 644,
    OF_OXM_BSN_IP_FRAGMENTATION_MASKED = 645,
    OF_OXM_BSN_L2_CACHE_HIT = 646,
    OF_OXM_BSN_L2_CACHE_HIT_MASKED = 647,
    OF_OXM_BSN_L3_DST_CLASS_ID = 648,
    OF_OXM_BSN_L3_DST_CLASS_ID_MASKED = 649,
    OF_OXM_BSN_L3_INTERFACE_CLASS_ID = 650,
    OF_OXM_BSN_L3_INTERFACE_CLASS_ID_MASKED = 651,
    OF_OXM_BSN_L3_SRC_CLASS_ID = 652,
    OF_OXM_BSN_L3_SRC_CLASS_ID_MASKED = 653,
    OF_OXM_BSN_LAG_ID = 654,
    OF_OXM_BSN_LAG_ID_MASKED = 655,
    OF_OXM_BSN_TCP_FLAGS = 656,
    OF_OXM_BSN_TCP_FLAGS_MASKED = 657,
    OF_OXM_BSN_UDF0 = 658,
    OF_OXM_BSN_UDF0_MASKED = 659,
    OF_OXM_BSN_UDF1 = 660,
    OF_OXM_BSN_UDF1_MASKED = 661,
    OF_OXM_BSN_UDF2 = 662,
    OF_OXM_BSN_UDF2_MASKED = 663,
    OF_OXM_BSN_UDF3 = 664,
    OF_OXM_BSN_UDF3_MASKED = 665,
    OF_OXM_BSN_UDF4 = 666,
    OF_OXM_BSN_UDF4_MASKED = 667,
    OF_OXM_BSN_UDF5 = 668,
    OF_OXM_BSN_UDF5_MASKED = 669,
    OF_OXM_BSN_UDF6 = 670,
    OF_OXM_BSN_UDF6_MASKED = 671,
    OF_OXM_BSN_UDF7 = 672,
    OF_OXM_BSN_UDF7_MASKED = 673,
    OF_OXM_BSN_VFI = 674,
    OF_OXM_BSN_VFI_MASKED = 675,
    OF_OXM_BSN_VLAN_XLATE_PORT_GROUP_ID = 676,
    OF_OXM_BSN_VLAN_XLATE_PORT_GROUP_ID_MASKED = 677,
    OF_OXM_BSN_VRF = 678,
    OF_OXM_BSN_VRF_MASKED = 679,
    OF_OXM_BSN_VXLAN_NETWORK_ID = 680,
    OF_OXM_BSN_VXLAN_NETWORK_ID_MASKED = 681,
    OF_OXM_CIRCUIT_OCH_SIGID = 682,
    OF_OXM_CIRCUIT_OCH_SIGID_BASIC = 683,
    OF_OXM_CIRCUIT_OCH_SIGID_BASIC_MASKED = 684,
    OF_OXM_CIRCUIT_OCH_SIGID_MASKED = 685,
    OF_OXM_CIRCUIT_OCH_SIGTYPE = 686,
    OF_OXM_CIRCUIT_OCH_SIGTYPE_BASIC = 687,
    OF_OXM_CIRCUIT_OCH_SIGTYPE_BASIC_MASKED = 688,
    OF_OXM_CIRCUIT_OCH_SIGTYPE_MASKED = 689,
    OF_OXM_ENCAP_ETH_DST = 690,
    OF_OXM_ENCAP_ETH_DST_MASKED = 691,
    OF_OXM_ENCAP_ETH_SRC = 692,
    OF_OXM_ENCAP_ETH_SRC_MASKED = 693,
    OF_OXM_ENCAP_ETH_TYPE = 694,
    OF_OXM_ENCAP_ETH_TYPE_MASKED = 695,
    OF_OXM_ETH_DST = 696,
    OF_OXM_ETH_DST_MASKED = 697,
    OF_OXM_ETH_SRC = 698,
    OF_OXM_ETH_SRC_MASKED = 699,
    OF_OXM_ETH_TYPE = 700,
    OF_OXM_ETH_TYPE_MASKED = 701,
    OF_OXM_EXP_OCH_SIGID = 702,
    OF_OXM_EXP_OCH_SIGID_MASKED = 703,
    OF_OXM_EXP_OCH_SIGTYPE = 704,
    OF_OXM_EXP_OCH_SIGTYPE_MASKED = 705,
    OF_OXM_EXP_ODU_SIGID = 706,
    OF_OXM_EXP_ODU_SIGID_MASKED = 707,
    OF_OXM_EXP_ODU_SIGTYPE = 708,
    OF_OXM_EXP_ODU_SIGTYPE_MASKED = 709,
    OF_OXM_ICMPV4_CODE = 710,
    OF_OXM_ICMPV4_CODE_MASKED = 711,
    OF_OXM_ICMPV4_TYPE = 712,
    OF_OXM_ICMPV4_TYPE_MASKED = 713,
    OF_OXM_ICMPV6_CODE = 714,
    OF_OXM_ICMPV6_CODE_MASKED = 715,
    OF_OXM_ICMPV6_TYPE = 716,
    OF_OXM_ICMPV6_TYPE_MASKED = 717,
    OF_OXM_IN_PHY_PORT = 718,
    OF_OXM_IN_PHY_PORT_MASKED = 719,
    OF_OXM_IN_PORT = 720,
    OF_OXM_IN_PORT_MASKED = 721,
    OF_OXM_IP_DSCP = 722,
    OF_OXM_IP_DSCP_MASKED = 723,
    OF_OXM_IP_ECN = 724,
    OF_OXM_IP_ECN_MASKED = 725,
    OF_OXM_IP_PROTO = 726,
    OF_OXM_IP_PROTO_MASKED = 727,
    OF_OXM_IPV4_DST = 728,
    OF_OXM_IPV4_DST_MASKED = 729,
    OF_OXM_IPV4_SRC = 730,
    OF_OXM_IPV4_SRC_MASKED = 731,
    OF_OXM_IPV6_DST = 732,
    OF_OXM_IPV6_DST_MASKED = 733,
    OF_OXM_IPV6_EXTHDR = 734,
    OF_OXM_IPV6_EXTHDR_MASKED = 735,
    OF_OXM_IPV6_FLABEL = 736,
    OF_OXM_IPV6_FLABEL_MASKED = 737,
    OF_OXM_IPV6_ND_SLL = 738,
    OF_OXM_IPV6_ND_SLL_MASKED = 739,
    OF_OXM_IPV6_ND_TARGET = 740,
    OF_OXM_IPV6_ND_TARGET_MASKED = 741,
    OF_OXM_IPV6_ND_TLL = 742,
    OF_OXM_IPV6_ND_TLL_MASKED = 743,
    OF_OXM_IPV6_SRC = 744,
    OF_OXM_IPV6_SRC_MASKED = 745,
    OF_OXM_METADATA = 746,
    OF_OXM_METADATA_MASKED = 747,
    OF_OXM_MPLS_BOS = 748,
    OF_OXM_MPLS_BOS_MASKED = 749,
    OF_OXM_MPLS_LABEL = 750,
    OF_OXM_MPLS_LABEL_MASKED = 751,
    OF_OXM_MPLS_TC = 752,
    OF_OXM_MPLS_TC_MASKED = 753,
    OF_OXM_NSH_C1 = 754,
    OF_OXM_NSH_C1_MASKED = 755,
    OF_OXM_NSH_C2 = 756,
    OF_OXM_NSH_C2_MASKED = 757,
    OF_OXM_NSH_C3 = 758,
    OF_OXM_NSH_C3_MASKED = 759,
    OF_OXM_NSH_C4 = 760,
    OF_OXM_NSH_C4_MASKED = 761,
    OF_OXM_NSH_MDTYPE = 762,
    OF_OXM_NSH_MDTYPE_MASKED = 763,
    OF_OXM_NSH_NP = 764,
    OF_OXM_NSH_NP_MASKED = 765,
    OF_OXM_NSI = 766,
    OF_OXM_NSI_MASKED = 767,
    OF_OXM_NSP = 768,
    OF_OXM_NSP_MASKED = 769,
    OF_OXM_OCH_SIGATT = 770,
    OF_OXM_OCH_SIGATT_BASIC = 771,
    OF_OXM_OCH_SIGATT_BASIC_MASKED = 772,
    OF_OXM_OCH_SIGATT_MASKED = 773,
    OF_OXM_OFDPA_MPLS_L2_PORT = 774,
    OF_OXM_OFDPA_MPLS_L2_PORT_MASKED = 775,
    OF_OXM_OFDPA_MPLS_TYPE = 776,
    OF_OXM_OFDPA_MPLS_TYPE_MASKED = 777,
    OF_OXM_OFDPA_OVID = 778,
    OF_OXM_OFDPA_OVID_MASKED = 779,
    OF_OXM_OFDPA_QOS_INDEX = 780,
    OF_OXM_OFDPA_QOS_INDEX_MASKED = 781,
    OF_OXM_OVS_TCP_FLAGS = 782,
    OF_OXM_OVS_TCP_FLAGS_MASKED = 783,
    OF_OXM_PBB_UCA = 784,
    OF_OXM_PBB_UCA_MASKED = 785,
    OF_OXM_SCTP_DST = 786,
    OF_OXM_SCTP_DST_MASKED = 787,
    OF_OXM_SCTP_SRC = 788,
    OF_OXM_SCTP_SRC_MASKED = 789,
    OF_OXM_TCP_DST = 790,
    OF_OXM_TCP_DST_MASKED = 791,
    OF_OXM_TCP_SRC = 792,
    OF_OXM_TCP_SRC_MASKED = 793,
    OF_OXM_TUN_FLAGS = 794,
    OF_OXM_TUN_FLAGS_MASKED = 795,
    OF_OXM_TUN_GBP_FLAGS = 796,
    OF_OXM_TUN_GBP_FLAGS_MASKED = 797,
    OF_OXM_TUN_GBP_ID = 798,
    OF_OXM_TUN_GBP_ID_MASKED = 799,
    OF_OXM_TUN_GPE_FLAGS = 800,
    OF_OXM_TUN_GPE_FLAGS_MASKED = 801,
    OF_OXM_TUN_GPE_NP = 802,
    OF_OXM_TUN_GPE_NP_MASKED = 803,
    OF_OXM_TUNNEL_ID = 804,
    OF_OXM_TUNNEL_ID_MASKED = 805,
    OF_OXM_TUNNEL_IPV4_DST = 806,
    OF_OXM_TUNNEL_IPV4_DST_MASKED = 807,
    OF_OXM_TUNNEL_IPV4_SRC = 808,
    OF_OXM_TUNNEL_IPV4_SRC_MASKED = 809,
    OF_OXM_UDP_DST = 810,
    OF_OXM_UDP_DST_MASKED = 811,
    OF_OXM_UDP_SRC = 812,
    OF_OXM_UDP_SRC_MASKED = 813,
    OF_OXM_VLAN_PCP = 814,
    OF_OXM_VLAN_PCP_MASKED = 815,
    OF_OXM_VLAN_VID = 816,
    OF_OXM_VLAN_VID_MASKED = 817,
    OF_PACKET_QUEUE = 818,
    OF_PORT_DESC = 819,
    OF_PORT_DESC_PROP = 820,
    OF_PORT_DESC_PROP_BSN = 821,
    OF_PORT_DESC_PROP_BSN_FORWARD_ERROR_CORRECTION = 822,
    OF_PORT_DESC_PROP_BSN_GENERATION_ID = 823,
    OF_PORT_DESC_PROP_BSN_UPLINK = 824,
    OF_PORT_DESC_PROP_ETHERNET = 825,
    OF_PORT_DESC_PROP_EXPERIMENTER = 826,
    OF_PORT_DESC_PROP_OPTICAL = 827,
    OF_PORT_DESC_PROP_OPTICAL_TRANSPORT = 828,
    OF_PORT_MOD_PROP = 829,
    OF_PORT_MOD_PROP_ETHERNET = 830,
    OF_PORT_MOD_PROP_EXPERIMENTER = 831,
    OF_PORT_MOD_PROP_OPTICAL = 832,
    OF_PORT_OPTICAL = 833,
    OF_PORT_OPTICAL_TRANSPORT_APPLICATION_CODE = 834,
    OF_PORT_OPTICAL_TRANSPORT_LAYER_ENTRY = 835,
    OF_PORT_OPTICAL_TRANSPORT_LAYER_STACK = 836,
    OF_PORT_STATS_ENTRY = 837,
    OF_PORT_STATS_PROP = 838,
    OF_PORT_STATS_PROP_ETHERNET = 839,
    OF_PORT_STATS_PROP_EXPERIMENTER = 840,
    OF_PORT_STATS_PROP_OPTICAL = 841,
    OF_QUEUE_DESC = 842,
    OF_QUEUE_DESC_PROP = 843,
    OF_QUEUE_DESC_PROP_BSN = 844,
    OF_QUEUE_DESC_PROP_BSN_QUEUE_NAME = 845,
    OF_QUEUE_DESC_PROP_EXPERIMENTER = 846,
    OF_QUEUE_DESC_PROP_MAX_RATE = 847,
    OF_QUEUE_DESC_PROP_MIN_RATE = 848,
    OF_QUEUE_PROP = 849,
    OF_QUEUE_PROP_EXPERIMENTER = 850,
    OF_QUEUE_PROP_MAX_RATE = 851,
    OF_QUEUE_PROP_MIN_RATE = 852,
    OF_QUEUE_STATS_ENTRY = 853,
    OF_QUEUE_STATS_PROP = 854,
    OF_QUEUE_STATS_PROP_EXPERIMENTER = 855,
    OF_ROLE_PROP = 856,
    OF_ROLE_PROP_EXPERIMENTER = 857,
    OF_SDWN_ENTITY = 858,
    OF_SDWN_ENTITY_ACCESSPOINT = 859,
    OF_SDWN_ENTITY_BAND = 860,
    OF_SDWN_ENTITY_FREQ = 861,
    OF_SDWN_ENTITY_NIC = 862,
    OF_SDWN_ENTITY_RATE = 863,
    OF_SDWN_ENTITY_RELATED_SWITCH = 864,
    OF_TABLE_DESC = 865,
    OF_TABLE_FEATURE_PROP = 866,
    OF_TABLE_FEATURE_PROP_APPLY_ACTIONS = 867,
    OF_TABLE_FEATURE_PROP_APPLY_ACTIONS_MISS = 868,
    OF_TABLE_FEATURE_PROP_APPLY_SETFIELD = 869,
    OF_TABLE_FEATURE_PROP_APPLY_SETFIELD_MISS = 870,
    OF_TABLE_FEATURE_PROP_EXPERIMENTER = 871,
    OF_TABLE_FEATURE_PROP_EXPERIMENTER_MISS = 872,
    OF_TABLE_FEATURE_PROP_INSTRUCTIONS = 873,
    OF_TABLE_FEATURE_PROP_INSTRUCTIONS_MISS = 874,
    OF_TABLE_FEATURE_PROP_MATCH = 875,
    OF_TABLE_FEATURE_PROP_NEXT_TABLES = 876,
    OF_TABLE_FEATURE_PROP_NEXT_TABLES_MISS = 877,
    OF_TABLE_FEATURE_PROP_TABLE_SYNC_FROM = 878,
    OF_TABLE_FEATURE_PROP_WILDCARDS = 879,
    OF_TABLE_FEATURE_PROP_WRITE_ACTIONS = 880,
    OF_TABLE_FEATURE_PROP_WRITE_ACTIONS_MISS = 881,
    OF_TABLE_FEATURE_PROP_WRITE_SETFIELD = 882,
    OF_TABLE_FEATURE_PROP_WRITE_SETFIELD_MISS = 883,
    OF_TABLE_FEATURES = 884,
    OF_TABLE_MOD_PROP = 885,
    OF_TABLE_MOD_PROP_EVICTION = 886,
    OF_TABLE_MOD_PROP_EXPERIMENTER = 887,
    OF_TABLE_MOD_PROP_VACANCY = 888,
    OF_TABLE_STATS_ENTRY = 889,
    OF_UINT32 = 890,
    OF_UINT64 = 891,
    OF_UINT8 = 892,

    /* List objects */
    OF_LIST_ACTION = 893,
    OF_LIST_ACTION_ID = 894,
    OF_LIST_ASYNC_CONFIG_PROP = 895,
    OF_LIST_BSN_CONTROLLER_CONNECTION = 896,
    OF_LIST_BSN_DEBUG_COUNTER_DESC_STATS_ENTRY = 897,
    OF_LIST_BSN_DEBUG_COUNTER_STATS_ENTRY = 898,
    OF_LIST_BSN_FLOW_CHECKSUM_BUCKET_STATS_ENTRY = 899,
    OF_LIST_BSN_GENERIC_STATS_ENTRY = 900,
    OF_LIST_BSN_GENTABLE_BUCKET_STATS_ENTRY = 901,
    OF_LIST_BSN_GENTABLE_DESC_STATS_ENTRY = 902,
    OF_LIST_BSN_GENTABLE_ENTRY_DESC_STATS_ENTRY = 903,
    OF_LIST_BSN_GENTABLE_ENTRY_STATS_ENTRY = 904,
    OF_LIST_BSN_GENTABLE_STATS_ENTRY = 905,
    OF_LIST_BSN_INTERFACE = 906,
    OF_LIST_BSN_LACP_STATS_ENTRY = 907,
    OF_LIST_BSN_PORT_COUNTER_STATS_ENTRY = 908,
    OF_LIST_BSN_SWITCH_PIPELINE_STATS_ENTRY = 909,
    OF_LIST_BSN_TABLE_CHECKSUM_STATS_ENTRY = 910,
    OF_LIST_BSN_TLV = 911,
    OF_LIST_BSN_TLV_DATA = 912,
    OF_LIST_BSN_VLAN_COUNTER_STATS_ENTRY = 913,
    OF_LIST_BSN_VLAN_MAC = 914,
    OF_LIST_BSN_VRF_COUNTER_STATS_ENTRY = 915,
    OF_LIST_BUCKET = 916,
    OF_LIST_BUCKET_COUNTER = 917,
    OF_LIST_BUNDLE_PROP = 918,
    OF_LIST_CALIENT_FLOW_STATS_ENTRY = 919,
    OF_LIST_CALIENT_PORT_DESC_PROP = 920,
    OF_LIST_CALIENT_PORT_DESC_STATS_ENTRY = 921,
    OF_LIST_CALIENT_PORT_STATS_ENTRY = 922,
    OF_LIST_EXP_EXT_AD_ID = 923,
    OF_LIST_EXP_PORT = 924,
    OF_LIST_EXP_PORT_ADJACENCY = 925,
    OF_LIST_EXP_PORT_ADJACENCY_ID = 926,
    OF_LIST_EXP_PORT_DESC_PROP_OPTICAL_TRANSPORT = 927,
    OF_LIST_EXP_PORT_OPTICAL_TRANSPORT_LAYER_ENTRY = 928,
    OF_LIST_EXP_PORT_OPTICAL_TRANSPORT_LAYER_STACK = 929,
    OF_LIST_FLOW_STATS_ENTRY = 930,
    OF_LIST_GROUP_DESC_STATS_ENTRY = 931,
    OF_LIST_GROUP_STATS_ENTRY = 932,
    OF_LIST_HELLO_ELEM = 933,
    OF_LIST_INSTRUCTION = 934,
    OF_LIST_INSTRUCTION_ID = 935,
    OF_LIST_METER_BAND = 936,
    OF_LIST_METER_BAND_STATS = 937,
    OF_LIST_METER_CONFIG = 938,
    OF_LIST_METER_STATS = 939,
    OF_LIST_OPLINK_CHANNEL_POWER = 940,
    OF_LIST_OPLINK_PORT_POWER = 941,
    OF_LIST_OXM = 942,
    OF_LIST_PACKET_QUEUE = 943,
    OF_LIST_PORT_DESC = 944,
    OF_LIST_PORT_DESC_PROP = 945,
    OF_LIST_PORT_DESC_PROP_OPTICAL_TRANSPORT = 946,
    OF_LIST_PORT_MOD_PROP = 947,
    OF_LIST_PORT_OPTICAL = 948,
    OF_LIST_PORT_OPTICAL_TRANSPORT_LAYER_ENTRY = 949,
    OF_LIST_PORT_STATS_ENTRY = 950,
    OF_LIST_PORT_STATS_PROP = 951,
    OF_LIST_QUEUE_DESC = 952,
    OF_LIST_QUEUE_DESC_PROP = 953,
    OF_LIST_QUEUE_PROP = 954,
    OF_LIST_QUEUE_STATS_ENTRY = 955,
    OF_LIST_QUEUE_STATS_PROP = 956,
    OF_LIST_ROLE_PROP = 957,
    OF_LIST_SDWN_ENTITY = 958,
    OF_LIST_TABLE_DESC = 959,
    OF_LIST_TABLE_FEATURE_PROP = 960,
    OF_LIST_TABLE_FEATURES = 961,
    OF_LIST_TABLE_MOD_PROP = 962,
    OF_LIST_TABLE_STATS_ENTRY = 963,
    OF_LIST_UINT32 = 964,
    OF_LIST_UINT64 = 965,
    OF_LIST_UINT8 = 966,

    OF_OBJECT_COUNT = 967
} of_object_id_t;

extern const char *const of_object_id_str[];

#define OF_MESSAGE_OBJECT_COUNT 256

/****************************************************************
 *
 * Experimenter IDs
 *
 ****************************************************************/

#define OF_EXPERIMENTER_ID_OPENFLOW 0x000026e1
#define OF_EXPERIMENTER_ID_NICIRA 0x00002320
#define OF_EXPERIMENTER_ID_BSN 0x005c16c7
#define OF_EXPERIMENTER_ID_SDWN 0x00421337

/****************************************************************
 *
 * OpenFlow Match version specific and generic defines
 *
 ****************************************************************/

/**
 * Definitions to coerce v4 match (version 1.3) to v3 matches
 * (version 1.2).
 * @FIXME This is a stopgap and needs to get cleaned up.
 */
#define of_match_v4_t of_match_v3_t
#define of_match_v4_init of_match_v3_init
#define of_match_v4_new of_match_v3_new
#define of_match_v4_to_match of_match_v3_to_match
#define of_match_to_wire_match_v4 of_match_to_wire_match_v3
#define of_match_v4_delete of_match_v3_delete

#define of_match_v5_t of_match_v3_t
#define of_match_v5_init of_match_v3_init
#define of_match_v5_new of_match_v3_new
#define of_match_v5_to_match of_match_v3_to_match
#define of_match_to_wire_match_v5 of_match_to_wire_match_v3
#define of_match_v5_delete of_match_v3_delete


/**
 * Definitions for wildcard macros for OF_VERSION_1_0
 */


#define OF_MATCH_V1_WC_ETH_DST_SHIFT 3
#define OF_MATCH_V1_WC_ETH_DST_MASK (1 << 3)
#define OF_MATCH_V1_WC_ETH_DST_SET(wc) ((wc) |= (1 << 3))
#define OF_MATCH_V1_WC_ETH_DST_CLEAR(wc) ((wc) &= ~(1 << 3))
#define OF_MATCH_V1_WC_ETH_DST_TEST(wc) ((wc) & (1 << 3))

#define OF_MATCH_V1_WC_ETH_SRC_SHIFT 2
#define OF_MATCH_V1_WC_ETH_SRC_MASK (1 << 2)
#define OF_MATCH_V1_WC_ETH_SRC_SET(wc) ((wc) |= (1 << 2))
#define OF_MATCH_V1_WC_ETH_SRC_CLEAR(wc) ((wc) &= ~(1 << 2))
#define OF_MATCH_V1_WC_ETH_SRC_TEST(wc) ((wc) & (1 << 2))

#define OF_MATCH_V1_WC_ETH_TYPE_SHIFT 4
#define OF_MATCH_V1_WC_ETH_TYPE_MASK (1 << 4)
#define OF_MATCH_V1_WC_ETH_TYPE_SET(wc) ((wc) |= (1 << 4))
#define OF_MATCH_V1_WC_ETH_TYPE_CLEAR(wc) ((wc) &= ~(1 << 4))
#define OF_MATCH_V1_WC_ETH_TYPE_TEST(wc) ((wc) & (1 << 4))

#define OF_MATCH_V1_WC_IN_PORT_SHIFT 0
#define OF_MATCH_V1_WC_IN_PORT_MASK (1 << 0)
#define OF_MATCH_V1_WC_IN_PORT_SET(wc) ((wc) |= (1 << 0))
#define OF_MATCH_V1_WC_IN_PORT_CLEAR(wc) ((wc) &= ~(1 << 0))
#define OF_MATCH_V1_WC_IN_PORT_TEST(wc) ((wc) & (1 << 0))

#define OF_MATCH_V1_WC_IPV4_DST_SHIFT 14
#define OF_MATCH_V1_WC_IPV4_DST_MASK (0x3f << 14)
#define OF_MATCH_V1_WC_IPV4_DST_CLEAR(wc) ((wc) &= ~(0x3f << 14))
#define OF_MATCH_V1_WC_IPV4_DST_SET(wc, value) do {   \
        OF_MATCH_V1_WC_IPV4_DST_CLEAR(wc); \
        ((wc) |= (((value) & 0x3f) << 14)); \
    } while (0)
#define OF_MATCH_V1_WC_IPV4_DST_TEST(wc) ((wc) & (0x3f << 14))
#define OF_MATCH_V1_WC_IPV4_DST_GET(wc) (((wc) >> 14) & 0x3f)

#define OF_MATCH_V1_WC_IP_PROTO_SHIFT 5
#define OF_MATCH_V1_WC_IP_PROTO_MASK (1 << 5)
#define OF_MATCH_V1_WC_IP_PROTO_SET(wc) ((wc) |= (1 << 5))
#define OF_MATCH_V1_WC_IP_PROTO_CLEAR(wc) ((wc) &= ~(1 << 5))
#define OF_MATCH_V1_WC_IP_PROTO_TEST(wc) ((wc) & (1 << 5))

#define OF_MATCH_V1_WC_IPV4_SRC_SHIFT 8
#define OF_MATCH_V1_WC_IPV4_SRC_MASK (0x3f << 8)
#define OF_MATCH_V1_WC_IPV4_SRC_CLEAR(wc) ((wc) &= ~(0x3f << 8))
#define OF_MATCH_V1_WC_IPV4_SRC_SET(wc, value) do {   \
        OF_MATCH_V1_WC_IPV4_SRC_CLEAR(wc); \
        ((wc) |= (((value) & 0x3f) << 8)); \
    } while (0)
#define OF_MATCH_V1_WC_IPV4_SRC_TEST(wc) ((wc) & (0x3f << 8))
#define OF_MATCH_V1_WC_IPV4_SRC_GET(wc) (((wc) >> 8) & 0x3f)

#define OF_MATCH_V1_WC_IP_DSCP_SHIFT 21
#define OF_MATCH_V1_WC_IP_DSCP_MASK (1 << 21)
#define OF_MATCH_V1_WC_IP_DSCP_SET(wc) ((wc) |= (1 << 21))
#define OF_MATCH_V1_WC_IP_DSCP_CLEAR(wc) ((wc) &= ~(1 << 21))
#define OF_MATCH_V1_WC_IP_DSCP_TEST(wc) ((wc) & (1 << 21))

#define OF_MATCH_V1_WC_TCP_DST_SHIFT 7
#define OF_MATCH_V1_WC_TCP_DST_MASK (1 << 7)
#define OF_MATCH_V1_WC_TCP_DST_SET(wc) ((wc) |= (1 << 7))
#define OF_MATCH_V1_WC_TCP_DST_CLEAR(wc) ((wc) &= ~(1 << 7))
#define OF_MATCH_V1_WC_TCP_DST_TEST(wc) ((wc) & (1 << 7))

#define OF_MATCH_V1_WC_TCP_SRC_SHIFT 6
#define OF_MATCH_V1_WC_TCP_SRC_MASK (1 << 6)
#define OF_MATCH_V1_WC_TCP_SRC_SET(wc) ((wc) |= (1 << 6))
#define OF_MATCH_V1_WC_TCP_SRC_CLEAR(wc) ((wc) &= ~(1 << 6))
#define OF_MATCH_V1_WC_TCP_SRC_TEST(wc) ((wc) & (1 << 6))

#define OF_MATCH_V1_WC_VLAN_PCP_SHIFT 20
#define OF_MATCH_V1_WC_VLAN_PCP_MASK (1 << 20)
#define OF_MATCH_V1_WC_VLAN_PCP_SET(wc) ((wc) |= (1 << 20))
#define OF_MATCH_V1_WC_VLAN_PCP_CLEAR(wc) ((wc) &= ~(1 << 20))
#define OF_MATCH_V1_WC_VLAN_PCP_TEST(wc) ((wc) & (1 << 20))

#define OF_MATCH_V1_WC_VLAN_VID_SHIFT 1
#define OF_MATCH_V1_WC_VLAN_VID_MASK (1 << 1)
#define OF_MATCH_V1_WC_VLAN_VID_SET(wc) ((wc) |= (1 << 1))
#define OF_MATCH_V1_WC_VLAN_VID_CLEAR(wc) ((wc) &= ~(1 << 1))
#define OF_MATCH_V1_WC_VLAN_VID_TEST(wc) ((wc) & (1 << 1))


/**
 * Definitions for wildcard macros for OF_VERSION_1_1
 */

#define OF_MATCH_V2_WC_ETH_TYPE_SHIFT 3
#define OF_MATCH_V2_WC_ETH_TYPE_MASK (1 << 3)
#define OF_MATCH_V2_WC_ETH_TYPE_SET(wc) ((wc) |= (1 << 3))
#define OF_MATCH_V2_WC_ETH_TYPE_CLEAR(wc) ((wc) &= ~(1 << 3))
#define OF_MATCH_V2_WC_ETH_TYPE_TEST(wc) ((wc) & (1 << 3))

#define OF_MATCH_V2_WC_IN_PORT_SHIFT 0
#define OF_MATCH_V2_WC_IN_PORT_MASK (1 << 0)
#define OF_MATCH_V2_WC_IN_PORT_SET(wc) ((wc) |= (1 << 0))
#define OF_MATCH_V2_WC_IN_PORT_CLEAR(wc) ((wc) &= ~(1 << 0))
#define OF_MATCH_V2_WC_IN_PORT_TEST(wc) ((wc) & (1 << 0))

#define OF_MATCH_V2_WC_IP_DSCP_SHIFT 4
#define OF_MATCH_V2_WC_IP_DSCP_MASK (1 << 4)
#define OF_MATCH_V2_WC_IP_DSCP_SET(wc) ((wc) |= (1 << 4))
#define OF_MATCH_V2_WC_IP_DSCP_CLEAR(wc) ((wc) &= ~(1 << 4))
#define OF_MATCH_V2_WC_IP_DSCP_TEST(wc) ((wc) & (1 << 4))

#define OF_MATCH_V2_WC_IP_PROTO_SHIFT 5
#define OF_MATCH_V2_WC_IP_PROTO_MASK (1 << 5)
#define OF_MATCH_V2_WC_IP_PROTO_SET(wc) ((wc) |= (1 << 5))
#define OF_MATCH_V2_WC_IP_PROTO_CLEAR(wc) ((wc) &= ~(1 << 5))
#define OF_MATCH_V2_WC_IP_PROTO_TEST(wc) ((wc) & (1 << 5))

#define OF_MATCH_V2_WC_MPLS_LABEL_SHIFT 8
#define OF_MATCH_V2_WC_MPLS_LABEL_MASK (1 << 8)
#define OF_MATCH_V2_WC_MPLS_LABEL_SET(wc) ((wc) |= (1 << 8))
#define OF_MATCH_V2_WC_MPLS_LABEL_CLEAR(wc) ((wc) &= ~(1 << 8))
#define OF_MATCH_V2_WC_MPLS_LABEL_TEST(wc) ((wc) & (1 << 8))

#define OF_MATCH_V2_WC_MPLS_TC_SHIFT 9
#define OF_MATCH_V2_WC_MPLS_TC_MASK (1 << 9)
#define OF_MATCH_V2_WC_MPLS_TC_SET(wc) ((wc) |= (1 << 9))
#define OF_MATCH_V2_WC_MPLS_TC_CLEAR(wc) ((wc) &= ~(1 << 9))
#define OF_MATCH_V2_WC_MPLS_TC_TEST(wc) ((wc) & (1 << 9))

#define OF_MATCH_V2_WC_TCP_DST_SHIFT 7
#define OF_MATCH_V2_WC_TCP_DST_MASK (1 << 7)
#define OF_MATCH_V2_WC_TCP_DST_SET(wc) ((wc) |= (1 << 7))
#define OF_MATCH_V2_WC_TCP_DST_CLEAR(wc) ((wc) &= ~(1 << 7))
#define OF_MATCH_V2_WC_TCP_DST_TEST(wc) ((wc) & (1 << 7))

#define OF_MATCH_V2_WC_TCP_SRC_SHIFT 6
#define OF_MATCH_V2_WC_TCP_SRC_MASK (1 << 6)
#define OF_MATCH_V2_WC_TCP_SRC_SET(wc) ((wc) |= (1 << 6))
#define OF_MATCH_V2_WC_TCP_SRC_CLEAR(wc) ((wc) &= ~(1 << 6))
#define OF_MATCH_V2_WC_TCP_SRC_TEST(wc) ((wc) & (1 << 6))

#define OF_MATCH_V2_WC_VLAN_PCP_SHIFT 2
#define OF_MATCH_V2_WC_VLAN_PCP_MASK (1 << 2)
#define OF_MATCH_V2_WC_VLAN_PCP_SET(wc) ((wc) |= (1 << 2))
#define OF_MATCH_V2_WC_VLAN_PCP_CLEAR(wc) ((wc) &= ~(1 << 2))
#define OF_MATCH_V2_WC_VLAN_PCP_TEST(wc) ((wc) & (1 << 2))

#define OF_MATCH_V2_WC_VLAN_VID_SHIFT 1
#define OF_MATCH_V2_WC_VLAN_VID_MASK (1 << 1)
#define OF_MATCH_V2_WC_VLAN_VID_SET(wc) ((wc) |= (1 << 1))
#define OF_MATCH_V2_WC_VLAN_VID_CLEAR(wc) ((wc) &= ~(1 << 1))
#define OF_MATCH_V2_WC_VLAN_VID_TEST(wc) ((wc) & (1 << 1))

/*
 * The generic match structure uses the OXM bit indices for it's
 * bitmasks for active and masked values
 */

/* Mask/value check/set macros for ipv6_exthdr */

/**
 * Set the mask for an exact match of ipv6_exthdr
 */
#define OF_MATCH_MASK_IPV6_EXTHDR_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ipv6_exthdr, 0xff, \
        sizeof(((_match)->masks).ipv6_exthdr))

/**
 * Clear the mask for ipv6_exthdr making that field inactive for the match
 */
#define OF_MATCH_MASK_IPV6_EXTHDR_CLEAR(_match) \
    MEMSET(&(_match)->masks.ipv6_exthdr, 0, \
        sizeof(((_match)->masks).ipv6_exthdr))

/**
 * Test whether the match is exact for ipv6_exthdr
 */
#define OF_MATCH_MASK_IPV6_EXTHDR_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ipv6_exthdr))

/**
 * Test whether key ipv6_exthdr is being checked in the match
 */
#define OF_MATCH_MASK_IPV6_EXTHDR_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ipv6_exthdr))


/* Mask/value check/set macros for arp_sha */

/**
 * Set the mask for an exact match of arp_sha
 */
#define OF_MATCH_MASK_ARP_SHA_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.arp_sha, 0xff, \
        sizeof(((_match)->masks).arp_sha))

/**
 * Clear the mask for arp_sha making that field inactive for the match
 */
#define OF_MATCH_MASK_ARP_SHA_CLEAR(_match) \
    MEMSET(&(_match)->masks.arp_sha, 0, \
        sizeof(((_match)->masks).arp_sha))

/**
 * Test whether the match is exact for arp_sha
 */
#define OF_MATCH_MASK_ARP_SHA_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).arp_sha))

/**
 * Test whether key arp_sha is being checked in the match
 */
#define OF_MATCH_MASK_ARP_SHA_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).arp_sha))


/* Mask/value check/set macros for ofdpa_qos_index */

/**
 * Set the mask for an exact match of ofdpa_qos_index
 */
#define OF_MATCH_MASK_OFDPA_QOS_INDEX_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ofdpa_qos_index, 0xff, \
        sizeof(((_match)->masks).ofdpa_qos_index))

/**
 * Clear the mask for ofdpa_qos_index making that field inactive for the match
 */
#define OF_MATCH_MASK_OFDPA_QOS_INDEX_CLEAR(_match) \
    MEMSET(&(_match)->masks.ofdpa_qos_index, 0, \
        sizeof(((_match)->masks).ofdpa_qos_index))

/**
 * Test whether the match is exact for ofdpa_qos_index
 */
#define OF_MATCH_MASK_OFDPA_QOS_INDEX_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ofdpa_qos_index))

/**
 * Test whether key ofdpa_qos_index is being checked in the match
 */
#define OF_MATCH_MASK_OFDPA_QOS_INDEX_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ofdpa_qos_index))


/* Mask/value check/set macros for eth_dst */

/**
 * Set the mask for an exact match of eth_dst
 */
#define OF_MATCH_MASK_ETH_DST_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.eth_dst, 0xff, \
        sizeof(((_match)->masks).eth_dst))

/**
 * Clear the mask for eth_dst making that field inactive for the match
 */
#define OF_MATCH_MASK_ETH_DST_CLEAR(_match) \
    MEMSET(&(_match)->masks.eth_dst, 0, \
        sizeof(((_match)->masks).eth_dst))

/**
 * Test whether the match is exact for eth_dst
 */
#define OF_MATCH_MASK_ETH_DST_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).eth_dst))

/**
 * Test whether key eth_dst is being checked in the match
 */
#define OF_MATCH_MASK_ETH_DST_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).eth_dst))


/* Mask/value check/set macros for exp_och_sigtype */

/**
 * Set the mask for an exact match of exp_och_sigtype
 */
#define OF_MATCH_MASK_EXP_OCH_SIGTYPE_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.exp_och_sigtype, 0xff, \
        sizeof(((_match)->masks).exp_och_sigtype))

/**
 * Clear the mask for exp_och_sigtype making that field inactive for the match
 */
#define OF_MATCH_MASK_EXP_OCH_SIGTYPE_CLEAR(_match) \
    MEMSET(&(_match)->masks.exp_och_sigtype, 0, \
        sizeof(((_match)->masks).exp_och_sigtype))

/**
 * Test whether the match is exact for exp_och_sigtype
 */
#define OF_MATCH_MASK_EXP_OCH_SIGTYPE_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).exp_och_sigtype))

/**
 * Test whether key exp_och_sigtype is being checked in the match
 */
#define OF_MATCH_MASK_EXP_OCH_SIGTYPE_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).exp_och_sigtype))


/* Mask/value check/set macros for ipv4_dst */

/**
 * Set the mask for an exact match of ipv4_dst
 */
#define OF_MATCH_MASK_IPV4_DST_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ipv4_dst, 0xff, \
        sizeof(((_match)->masks).ipv4_dst))

/**
 * Clear the mask for ipv4_dst making that field inactive for the match
 */
#define OF_MATCH_MASK_IPV4_DST_CLEAR(_match) \
    MEMSET(&(_match)->masks.ipv4_dst, 0, \
        sizeof(((_match)->masks).ipv4_dst))

/**
 * Test whether the match is exact for ipv4_dst
 */
#define OF_MATCH_MASK_IPV4_DST_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ipv4_dst))

/**
 * Test whether key ipv4_dst is being checked in the match
 */
#define OF_MATCH_MASK_IPV4_DST_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ipv4_dst))


/* Mask/value check/set macros for exp_odu_sigid */

/**
 * Set the mask for an exact match of exp_odu_sigid
 */
#define OF_MATCH_MASK_EXP_ODU_SIGID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.exp_odu_sigid, 0xff, \
        sizeof(((_match)->masks).exp_odu_sigid))

/**
 * Clear the mask for exp_odu_sigid making that field inactive for the match
 */
#define OF_MATCH_MASK_EXP_ODU_SIGID_CLEAR(_match) \
    MEMSET(&(_match)->masks.exp_odu_sigid, 0, \
        sizeof(((_match)->masks).exp_odu_sigid))

/**
 * Test whether the match is exact for exp_odu_sigid
 */
#define OF_MATCH_MASK_EXP_ODU_SIGID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).exp_odu_sigid))

/**
 * Test whether key exp_odu_sigid is being checked in the match
 */
#define OF_MATCH_MASK_EXP_ODU_SIGID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).exp_odu_sigid))


/* Mask/value check/set macros for tunnel_id */

/**
 * Set the mask for an exact match of tunnel_id
 */
#define OF_MATCH_MASK_TUNNEL_ID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.tunnel_id, 0xff, \
        sizeof(((_match)->masks).tunnel_id))

/**
 * Clear the mask for tunnel_id making that field inactive for the match
 */
#define OF_MATCH_MASK_TUNNEL_ID_CLEAR(_match) \
    MEMSET(&(_match)->masks.tunnel_id, 0, \
        sizeof(((_match)->masks).tunnel_id))

/**
 * Test whether the match is exact for tunnel_id
 */
#define OF_MATCH_MASK_TUNNEL_ID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).tunnel_id))

/**
 * Test whether key tunnel_id is being checked in the match
 */
#define OF_MATCH_MASK_TUNNEL_ID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).tunnel_id))


/* Mask/value check/set macros for arp_op */

/**
 * Set the mask for an exact match of arp_op
 */
#define OF_MATCH_MASK_ARP_OP_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.arp_op, 0xff, \
        sizeof(((_match)->masks).arp_op))

/**
 * Clear the mask for arp_op making that field inactive for the match
 */
#define OF_MATCH_MASK_ARP_OP_CLEAR(_match) \
    MEMSET(&(_match)->masks.arp_op, 0, \
        sizeof(((_match)->masks).arp_op))

/**
 * Test whether the match is exact for arp_op
 */
#define OF_MATCH_MASK_ARP_OP_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).arp_op))

/**
 * Test whether key arp_op is being checked in the match
 */
#define OF_MATCH_MASK_ARP_OP_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).arp_op))


/* Mask/value check/set macros for eth_type */

/**
 * Set the mask for an exact match of eth_type
 */
#define OF_MATCH_MASK_ETH_TYPE_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.eth_type, 0xff, \
        sizeof(((_match)->masks).eth_type))

/**
 * Clear the mask for eth_type making that field inactive for the match
 */
#define OF_MATCH_MASK_ETH_TYPE_CLEAR(_match) \
    MEMSET(&(_match)->masks.eth_type, 0, \
        sizeof(((_match)->masks).eth_type))

/**
 * Test whether the match is exact for eth_type
 */
#define OF_MATCH_MASK_ETH_TYPE_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).eth_type))

/**
 * Test whether key eth_type is being checked in the match
 */
#define OF_MATCH_MASK_ETH_TYPE_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).eth_type))


/* Mask/value check/set macros for mpls_bos */

/**
 * Set the mask for an exact match of mpls_bos
 */
#define OF_MATCH_MASK_MPLS_BOS_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.mpls_bos, 0xff, \
        sizeof(((_match)->masks).mpls_bos))

/**
 * Clear the mask for mpls_bos making that field inactive for the match
 */
#define OF_MATCH_MASK_MPLS_BOS_CLEAR(_match) \
    MEMSET(&(_match)->masks.mpls_bos, 0, \
        sizeof(((_match)->masks).mpls_bos))

/**
 * Test whether the match is exact for mpls_bos
 */
#define OF_MATCH_MASK_MPLS_BOS_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).mpls_bos))

/**
 * Test whether key mpls_bos is being checked in the match
 */
#define OF_MATCH_MASK_MPLS_BOS_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).mpls_bos))


/* Mask/value check/set macros for circuit_och_sigid */

/**
 * Set the mask for an exact match of circuit_och_sigid
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.circuit_och_sigid, 0xff, \
        sizeof(((_match)->masks).circuit_och_sigid))

/**
 * Clear the mask for circuit_och_sigid making that field inactive for the match
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGID_CLEAR(_match) \
    MEMSET(&(_match)->masks.circuit_och_sigid, 0, \
        sizeof(((_match)->masks).circuit_och_sigid))

/**
 * Test whether the match is exact for circuit_och_sigid
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).circuit_och_sigid))

/**
 * Test whether key circuit_och_sigid is being checked in the match
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).circuit_och_sigid))


/* Mask/value check/set macros for arp_spa */

/**
 * Set the mask for an exact match of arp_spa
 */
#define OF_MATCH_MASK_ARP_SPA_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.arp_spa, 0xff, \
        sizeof(((_match)->masks).arp_spa))

/**
 * Clear the mask for arp_spa making that field inactive for the match
 */
#define OF_MATCH_MASK_ARP_SPA_CLEAR(_match) \
    MEMSET(&(_match)->masks.arp_spa, 0, \
        sizeof(((_match)->masks).arp_spa))

/**
 * Test whether the match is exact for arp_spa
 */
#define OF_MATCH_MASK_ARP_SPA_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).arp_spa))

/**
 * Test whether key arp_spa is being checked in the match
 */
#define OF_MATCH_MASK_ARP_SPA_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).arp_spa))


/* Mask/value check/set macros for tun_flags */

/**
 * Set the mask for an exact match of tun_flags
 */
#define OF_MATCH_MASK_TUN_FLAGS_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.tun_flags, 0xff, \
        sizeof(((_match)->masks).tun_flags))

/**
 * Clear the mask for tun_flags making that field inactive for the match
 */
#define OF_MATCH_MASK_TUN_FLAGS_CLEAR(_match) \
    MEMSET(&(_match)->masks.tun_flags, 0, \
        sizeof(((_match)->masks).tun_flags))

/**
 * Test whether the match is exact for tun_flags
 */
#define OF_MATCH_MASK_TUN_FLAGS_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).tun_flags))

/**
 * Test whether key tun_flags is being checked in the match
 */
#define OF_MATCH_MASK_TUN_FLAGS_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).tun_flags))


/* Mask/value check/set macros for udp_dst */

/**
 * Set the mask for an exact match of udp_dst
 */
#define OF_MATCH_MASK_UDP_DST_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.udp_dst, 0xff, \
        sizeof(((_match)->masks).udp_dst))

/**
 * Clear the mask for udp_dst making that field inactive for the match
 */
#define OF_MATCH_MASK_UDP_DST_CLEAR(_match) \
    MEMSET(&(_match)->masks.udp_dst, 0, \
        sizeof(((_match)->masks).udp_dst))

/**
 * Test whether the match is exact for udp_dst
 */
#define OF_MATCH_MASK_UDP_DST_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).udp_dst))

/**
 * Test whether key udp_dst is being checked in the match
 */
#define OF_MATCH_MASK_UDP_DST_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).udp_dst))


/* Mask/value check/set macros for ipv6_nd_target */

/**
 * Set the mask for an exact match of ipv6_nd_target
 */
#define OF_MATCH_MASK_IPV6_ND_TARGET_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ipv6_nd_target, 0xff, \
        sizeof(((_match)->masks).ipv6_nd_target))

/**
 * Clear the mask for ipv6_nd_target making that field inactive for the match
 */
#define OF_MATCH_MASK_IPV6_ND_TARGET_CLEAR(_match) \
    MEMSET(&(_match)->masks.ipv6_nd_target, 0, \
        sizeof(((_match)->masks).ipv6_nd_target))

/**
 * Test whether the match is exact for ipv6_nd_target
 */
#define OF_MATCH_MASK_IPV6_ND_TARGET_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ipv6_nd_target))

/**
 * Test whether key ipv6_nd_target is being checked in the match
 */
#define OF_MATCH_MASK_IPV6_ND_TARGET_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ipv6_nd_target))


/* Mask/value check/set macros for tun_gpe_flags */

/**
 * Set the mask for an exact match of tun_gpe_flags
 */
#define OF_MATCH_MASK_TUN_GPE_FLAGS_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.tun_gpe_flags, 0xff, \
        sizeof(((_match)->masks).tun_gpe_flags))

/**
 * Clear the mask for tun_gpe_flags making that field inactive for the match
 */
#define OF_MATCH_MASK_TUN_GPE_FLAGS_CLEAR(_match) \
    MEMSET(&(_match)->masks.tun_gpe_flags, 0, \
        sizeof(((_match)->masks).tun_gpe_flags))

/**
 * Test whether the match is exact for tun_gpe_flags
 */
#define OF_MATCH_MASK_TUN_GPE_FLAGS_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).tun_gpe_flags))

/**
 * Test whether key tun_gpe_flags is being checked in the match
 */
#define OF_MATCH_MASK_TUN_GPE_FLAGS_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).tun_gpe_flags))


/* Mask/value check/set macros for icmpv4_type */

/**
 * Set the mask for an exact match of icmpv4_type
 */
#define OF_MATCH_MASK_ICMPV4_TYPE_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.icmpv4_type, 0xff, \
        sizeof(((_match)->masks).icmpv4_type))

/**
 * Clear the mask for icmpv4_type making that field inactive for the match
 */
#define OF_MATCH_MASK_ICMPV4_TYPE_CLEAR(_match) \
    MEMSET(&(_match)->masks.icmpv4_type, 0, \
        sizeof(((_match)->masks).icmpv4_type))

/**
 * Test whether the match is exact for icmpv4_type
 */
#define OF_MATCH_MASK_ICMPV4_TYPE_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).icmpv4_type))

/**
 * Test whether key icmpv4_type is being checked in the match
 */
#define OF_MATCH_MASK_ICMPV4_TYPE_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).icmpv4_type))


/* Mask/value check/set macros for tun_gbp_id */

/**
 * Set the mask for an exact match of tun_gbp_id
 */
#define OF_MATCH_MASK_TUN_GBP_ID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.tun_gbp_id, 0xff, \
        sizeof(((_match)->masks).tun_gbp_id))

/**
 * Clear the mask for tun_gbp_id making that field inactive for the match
 */
#define OF_MATCH_MASK_TUN_GBP_ID_CLEAR(_match) \
    MEMSET(&(_match)->masks.tun_gbp_id, 0, \
        sizeof(((_match)->masks).tun_gbp_id))

/**
 * Test whether the match is exact for tun_gbp_id
 */
#define OF_MATCH_MASK_TUN_GBP_ID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).tun_gbp_id))

/**
 * Test whether key tun_gbp_id is being checked in the match
 */
#define OF_MATCH_MASK_TUN_GBP_ID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).tun_gbp_id))


/* Mask/value check/set macros for tcp_dst */

/**
 * Set the mask for an exact match of tcp_dst
 */
#define OF_MATCH_MASK_TCP_DST_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.tcp_dst, 0xff, \
        sizeof(((_match)->masks).tcp_dst))

/**
 * Clear the mask for tcp_dst making that field inactive for the match
 */
#define OF_MATCH_MASK_TCP_DST_CLEAR(_match) \
    MEMSET(&(_match)->masks.tcp_dst, 0, \
        sizeof(((_match)->masks).tcp_dst))

/**
 * Test whether the match is exact for tcp_dst
 */
#define OF_MATCH_MASK_TCP_DST_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).tcp_dst))

/**
 * Test whether key tcp_dst is being checked in the match
 */
#define OF_MATCH_MASK_TCP_DST_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).tcp_dst))


/* Mask/value check/set macros for ip_proto */

/**
 * Set the mask for an exact match of ip_proto
 */
#define OF_MATCH_MASK_IP_PROTO_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ip_proto, 0xff, \
        sizeof(((_match)->masks).ip_proto))

/**
 * Clear the mask for ip_proto making that field inactive for the match
 */
#define OF_MATCH_MASK_IP_PROTO_CLEAR(_match) \
    MEMSET(&(_match)->masks.ip_proto, 0, \
        sizeof(((_match)->masks).ip_proto))

/**
 * Test whether the match is exact for ip_proto
 */
#define OF_MATCH_MASK_IP_PROTO_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ip_proto))

/**
 * Test whether key ip_proto is being checked in the match
 */
#define OF_MATCH_MASK_IP_PROTO_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ip_proto))


/* Mask/value check/set macros for ip_dscp */

/**
 * Set the mask for an exact match of ip_dscp
 */
#define OF_MATCH_MASK_IP_DSCP_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ip_dscp, 0xff, \
        sizeof(((_match)->masks).ip_dscp))

/**
 * Clear the mask for ip_dscp making that field inactive for the match
 */
#define OF_MATCH_MASK_IP_DSCP_CLEAR(_match) \
    MEMSET(&(_match)->masks.ip_dscp, 0, \
        sizeof(((_match)->masks).ip_dscp))

/**
 * Test whether the match is exact for ip_dscp
 */
#define OF_MATCH_MASK_IP_DSCP_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ip_dscp))

/**
 * Test whether key ip_dscp is being checked in the match
 */
#define OF_MATCH_MASK_IP_DSCP_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ip_dscp))


/* Mask/value check/set macros for ipv6_flabel */

/**
 * Set the mask for an exact match of ipv6_flabel
 */
#define OF_MATCH_MASK_IPV6_FLABEL_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ipv6_flabel, 0xff, \
        sizeof(((_match)->masks).ipv6_flabel))

/**
 * Clear the mask for ipv6_flabel making that field inactive for the match
 */
#define OF_MATCH_MASK_IPV6_FLABEL_CLEAR(_match) \
    MEMSET(&(_match)->masks.ipv6_flabel, 0, \
        sizeof(((_match)->masks).ipv6_flabel))

/**
 * Test whether the match is exact for ipv6_flabel
 */
#define OF_MATCH_MASK_IPV6_FLABEL_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ipv6_flabel))

/**
 * Test whether key ipv6_flabel is being checked in the match
 */
#define OF_MATCH_MASK_IPV6_FLABEL_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ipv6_flabel))


/* Mask/value check/set macros for exp_och_sigid */

/**
 * Set the mask for an exact match of exp_och_sigid
 */
#define OF_MATCH_MASK_EXP_OCH_SIGID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.exp_och_sigid, 0xff, \
        sizeof(((_match)->masks).exp_och_sigid))

/**
 * Clear the mask for exp_och_sigid making that field inactive for the match
 */
#define OF_MATCH_MASK_EXP_OCH_SIGID_CLEAR(_match) \
    MEMSET(&(_match)->masks.exp_och_sigid, 0, \
        sizeof(((_match)->masks).exp_och_sigid))

/**
 * Test whether the match is exact for exp_och_sigid
 */
#define OF_MATCH_MASK_EXP_OCH_SIGID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).exp_och_sigid))

/**
 * Test whether key exp_och_sigid is being checked in the match
 */
#define OF_MATCH_MASK_EXP_OCH_SIGID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).exp_och_sigid))


/* Mask/value check/set macros for exp_odu_sigtype */

/**
 * Set the mask for an exact match of exp_odu_sigtype
 */
#define OF_MATCH_MASK_EXP_ODU_SIGTYPE_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.exp_odu_sigtype, 0xff, \
        sizeof(((_match)->masks).exp_odu_sigtype))

/**
 * Clear the mask for exp_odu_sigtype making that field inactive for the match
 */
#define OF_MATCH_MASK_EXP_ODU_SIGTYPE_CLEAR(_match) \
    MEMSET(&(_match)->masks.exp_odu_sigtype, 0, \
        sizeof(((_match)->masks).exp_odu_sigtype))

/**
 * Test whether the match is exact for exp_odu_sigtype
 */
#define OF_MATCH_MASK_EXP_ODU_SIGTYPE_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).exp_odu_sigtype))

/**
 * Test whether key exp_odu_sigtype is being checked in the match
 */
#define OF_MATCH_MASK_EXP_ODU_SIGTYPE_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).exp_odu_sigtype))


/* Mask/value check/set macros for arp_tpa */

/**
 * Set the mask for an exact match of arp_tpa
 */
#define OF_MATCH_MASK_ARP_TPA_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.arp_tpa, 0xff, \
        sizeof(((_match)->masks).arp_tpa))

/**
 * Clear the mask for arp_tpa making that field inactive for the match
 */
#define OF_MATCH_MASK_ARP_TPA_CLEAR(_match) \
    MEMSET(&(_match)->masks.arp_tpa, 0, \
        sizeof(((_match)->masks).arp_tpa))

/**
 * Test whether the match is exact for arp_tpa
 */
#define OF_MATCH_MASK_ARP_TPA_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).arp_tpa))

/**
 * Test whether key arp_tpa is being checked in the match
 */
#define OF_MATCH_MASK_ARP_TPA_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).arp_tpa))


/* Mask/value check/set macros for nsh_c4 */

/**
 * Set the mask for an exact match of nsh_c4
 */
#define OF_MATCH_MASK_NSH_C4_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.nsh_c4, 0xff, \
        sizeof(((_match)->masks).nsh_c4))

/**
 * Clear the mask for nsh_c4 making that field inactive for the match
 */
#define OF_MATCH_MASK_NSH_C4_CLEAR(_match) \
    MEMSET(&(_match)->masks.nsh_c4, 0, \
        sizeof(((_match)->masks).nsh_c4))

/**
 * Test whether the match is exact for nsh_c4
 */
#define OF_MATCH_MASK_NSH_C4_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).nsh_c4))

/**
 * Test whether key nsh_c4 is being checked in the match
 */
#define OF_MATCH_MASK_NSH_C4_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).nsh_c4))


/* Mask/value check/set macros for nsh_c1 */

/**
 * Set the mask for an exact match of nsh_c1
 */
#define OF_MATCH_MASK_NSH_C1_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.nsh_c1, 0xff, \
        sizeof(((_match)->masks).nsh_c1))

/**
 * Clear the mask for nsh_c1 making that field inactive for the match
 */
#define OF_MATCH_MASK_NSH_C1_CLEAR(_match) \
    MEMSET(&(_match)->masks.nsh_c1, 0, \
        sizeof(((_match)->masks).nsh_c1))

/**
 * Test whether the match is exact for nsh_c1
 */
#define OF_MATCH_MASK_NSH_C1_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).nsh_c1))

/**
 * Test whether key nsh_c1 is being checked in the match
 */
#define OF_MATCH_MASK_NSH_C1_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).nsh_c1))


/* Mask/value check/set macros for nsh_c3 */

/**
 * Set the mask for an exact match of nsh_c3
 */
#define OF_MATCH_MASK_NSH_C3_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.nsh_c3, 0xff, \
        sizeof(((_match)->masks).nsh_c3))

/**
 * Clear the mask for nsh_c3 making that field inactive for the match
 */
#define OF_MATCH_MASK_NSH_C3_CLEAR(_match) \
    MEMSET(&(_match)->masks.nsh_c3, 0, \
        sizeof(((_match)->masks).nsh_c3))

/**
 * Test whether the match is exact for nsh_c3
 */
#define OF_MATCH_MASK_NSH_C3_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).nsh_c3))

/**
 * Test whether key nsh_c3 is being checked in the match
 */
#define OF_MATCH_MASK_NSH_C3_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).nsh_c3))


/* Mask/value check/set macros for nsh_c2 */

/**
 * Set the mask for an exact match of nsh_c2
 */
#define OF_MATCH_MASK_NSH_C2_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.nsh_c2, 0xff, \
        sizeof(((_match)->masks).nsh_c2))

/**
 * Clear the mask for nsh_c2 making that field inactive for the match
 */
#define OF_MATCH_MASK_NSH_C2_CLEAR(_match) \
    MEMSET(&(_match)->masks.nsh_c2, 0, \
        sizeof(((_match)->masks).nsh_c2))

/**
 * Test whether the match is exact for nsh_c2
 */
#define OF_MATCH_MASK_NSH_C2_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).nsh_c2))

/**
 * Test whether key nsh_c2 is being checked in the match
 */
#define OF_MATCH_MASK_NSH_C2_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).nsh_c2))


/* Mask/value check/set macros for ipv6_src */

/**
 * Set the mask for an exact match of ipv6_src
 */
#define OF_MATCH_MASK_IPV6_SRC_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ipv6_src, 0xff, \
        sizeof(((_match)->masks).ipv6_src))

/**
 * Clear the mask for ipv6_src making that field inactive for the match
 */
#define OF_MATCH_MASK_IPV6_SRC_CLEAR(_match) \
    MEMSET(&(_match)->masks.ipv6_src, 0, \
        sizeof(((_match)->masks).ipv6_src))

/**
 * Test whether the match is exact for ipv6_src
 */
#define OF_MATCH_MASK_IPV6_SRC_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ipv6_src))

/**
 * Test whether key ipv6_src is being checked in the match
 */
#define OF_MATCH_MASK_IPV6_SRC_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ipv6_src))


/* Mask/value check/set macros for ipv6_nd_sll */

/**
 * Set the mask for an exact match of ipv6_nd_sll
 */
#define OF_MATCH_MASK_IPV6_ND_SLL_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ipv6_nd_sll, 0xff, \
        sizeof(((_match)->masks).ipv6_nd_sll))

/**
 * Clear the mask for ipv6_nd_sll making that field inactive for the match
 */
#define OF_MATCH_MASK_IPV6_ND_SLL_CLEAR(_match) \
    MEMSET(&(_match)->masks.ipv6_nd_sll, 0, \
        sizeof(((_match)->masks).ipv6_nd_sll))

/**
 * Test whether the match is exact for ipv6_nd_sll
 */
#define OF_MATCH_MASK_IPV6_ND_SLL_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ipv6_nd_sll))

/**
 * Test whether key ipv6_nd_sll is being checked in the match
 */
#define OF_MATCH_MASK_IPV6_ND_SLL_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ipv6_nd_sll))


/* Mask/value check/set macros for och_sigatt */

/**
 * Set the mask for an exact match of och_sigatt
 */
#define OF_MATCH_MASK_OCH_SIGATT_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.och_sigatt, 0xff, \
        sizeof(((_match)->masks).och_sigatt))

/**
 * Clear the mask for och_sigatt making that field inactive for the match
 */
#define OF_MATCH_MASK_OCH_SIGATT_CLEAR(_match) \
    MEMSET(&(_match)->masks.och_sigatt, 0, \
        sizeof(((_match)->masks).och_sigatt))

/**
 * Test whether the match is exact for och_sigatt
 */
#define OF_MATCH_MASK_OCH_SIGATT_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).och_sigatt))

/**
 * Test whether key och_sigatt is being checked in the match
 */
#define OF_MATCH_MASK_OCH_SIGATT_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).och_sigatt))


/* Mask/value check/set macros for bsn_ingress_port_group_id */

/**
 * Set the mask for an exact match of bsn_ingress_port_group_id
 */
#define OF_MATCH_MASK_BSN_INGRESS_PORT_GROUP_ID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_ingress_port_group_id, 0xff, \
        sizeof(((_match)->masks).bsn_ingress_port_group_id))

/**
 * Clear the mask for bsn_ingress_port_group_id making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_INGRESS_PORT_GROUP_ID_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_ingress_port_group_id, 0, \
        sizeof(((_match)->masks).bsn_ingress_port_group_id))

/**
 * Test whether the match is exact for bsn_ingress_port_group_id
 */
#define OF_MATCH_MASK_BSN_INGRESS_PORT_GROUP_ID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_ingress_port_group_id))

/**
 * Test whether key bsn_ingress_port_group_id is being checked in the match
 */
#define OF_MATCH_MASK_BSN_INGRESS_PORT_GROUP_ID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_ingress_port_group_id))


/* Mask/value check/set macros for bsn_egr_port_group_id */

/**
 * Set the mask for an exact match of bsn_egr_port_group_id
 */
#define OF_MATCH_MASK_BSN_EGR_PORT_GROUP_ID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_egr_port_group_id, 0xff, \
        sizeof(((_match)->masks).bsn_egr_port_group_id))

/**
 * Clear the mask for bsn_egr_port_group_id making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_EGR_PORT_GROUP_ID_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_egr_port_group_id, 0, \
        sizeof(((_match)->masks).bsn_egr_port_group_id))

/**
 * Test whether the match is exact for bsn_egr_port_group_id
 */
#define OF_MATCH_MASK_BSN_EGR_PORT_GROUP_ID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_egr_port_group_id))

/**
 * Test whether key bsn_egr_port_group_id is being checked in the match
 */
#define OF_MATCH_MASK_BSN_EGR_PORT_GROUP_ID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_egr_port_group_id))


/* Mask/value check/set macros for vlan_vid */

/**
 * Set the mask for an exact match of vlan_vid
 */
#define OF_MATCH_MASK_VLAN_VID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.vlan_vid, 0xff, \
        sizeof(((_match)->masks).vlan_vid))

/**
 * Clear the mask for vlan_vid making that field inactive for the match
 */
#define OF_MATCH_MASK_VLAN_VID_CLEAR(_match) \
    MEMSET(&(_match)->masks.vlan_vid, 0, \
        sizeof(((_match)->masks).vlan_vid))

/**
 * Test whether the match is exact for vlan_vid
 */
#define OF_MATCH_MASK_VLAN_VID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).vlan_vid))

/**
 * Test whether key vlan_vid is being checked in the match
 */
#define OF_MATCH_MASK_VLAN_VID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).vlan_vid))


/* Mask/value check/set macros for arp_tha */

/**
 * Set the mask for an exact match of arp_tha
 */
#define OF_MATCH_MASK_ARP_THA_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.arp_tha, 0xff, \
        sizeof(((_match)->masks).arp_tha))

/**
 * Clear the mask for arp_tha making that field inactive for the match
 */
#define OF_MATCH_MASK_ARP_THA_CLEAR(_match) \
    MEMSET(&(_match)->masks.arp_tha, 0, \
        sizeof(((_match)->masks).arp_tha))

/**
 * Test whether the match is exact for arp_tha
 */
#define OF_MATCH_MASK_ARP_THA_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).arp_tha))

/**
 * Test whether key arp_tha is being checked in the match
 */
#define OF_MATCH_MASK_ARP_THA_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).arp_tha))


/* Mask/value check/set macros for bsn_vfi */

/**
 * Set the mask for an exact match of bsn_vfi
 */
#define OF_MATCH_MASK_BSN_VFI_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_vfi, 0xff, \
        sizeof(((_match)->masks).bsn_vfi))

/**
 * Clear the mask for bsn_vfi making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_VFI_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_vfi, 0, \
        sizeof(((_match)->masks).bsn_vfi))

/**
 * Test whether the match is exact for bsn_vfi
 */
#define OF_MATCH_MASK_BSN_VFI_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_vfi))

/**
 * Test whether key bsn_vfi is being checked in the match
 */
#define OF_MATCH_MASK_BSN_VFI_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_vfi))


/* Mask/value check/set macros for metadata */

/**
 * Set the mask for an exact match of metadata
 */
#define OF_MATCH_MASK_METADATA_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.metadata, 0xff, \
        sizeof(((_match)->masks).metadata))

/**
 * Clear the mask for metadata making that field inactive for the match
 */
#define OF_MATCH_MASK_METADATA_CLEAR(_match) \
    MEMSET(&(_match)->masks.metadata, 0, \
        sizeof(((_match)->masks).metadata))

/**
 * Test whether the match is exact for metadata
 */
#define OF_MATCH_MASK_METADATA_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).metadata))

/**
 * Test whether key metadata is being checked in the match
 */
#define OF_MATCH_MASK_METADATA_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).metadata))


/* Mask/value check/set macros for tunnel_ipv4_dst */

/**
 * Set the mask for an exact match of tunnel_ipv4_dst
 */
#define OF_MATCH_MASK_TUNNEL_IPV4_DST_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.tunnel_ipv4_dst, 0xff, \
        sizeof(((_match)->masks).tunnel_ipv4_dst))

/**
 * Clear the mask for tunnel_ipv4_dst making that field inactive for the match
 */
#define OF_MATCH_MASK_TUNNEL_IPV4_DST_CLEAR(_match) \
    MEMSET(&(_match)->masks.tunnel_ipv4_dst, 0, \
        sizeof(((_match)->masks).tunnel_ipv4_dst))

/**
 * Test whether the match is exact for tunnel_ipv4_dst
 */
#define OF_MATCH_MASK_TUNNEL_IPV4_DST_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).tunnel_ipv4_dst))

/**
 * Test whether key tunnel_ipv4_dst is being checked in the match
 */
#define OF_MATCH_MASK_TUNNEL_IPV4_DST_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).tunnel_ipv4_dst))


/* Mask/value check/set macros for sctp_dst */

/**
 * Set the mask for an exact match of sctp_dst
 */
#define OF_MATCH_MASK_SCTP_DST_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.sctp_dst, 0xff, \
        sizeof(((_match)->masks).sctp_dst))

/**
 * Clear the mask for sctp_dst making that field inactive for the match
 */
#define OF_MATCH_MASK_SCTP_DST_CLEAR(_match) \
    MEMSET(&(_match)->masks.sctp_dst, 0, \
        sizeof(((_match)->masks).sctp_dst))

/**
 * Test whether the match is exact for sctp_dst
 */
#define OF_MATCH_MASK_SCTP_DST_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).sctp_dst))

/**
 * Test whether key sctp_dst is being checked in the match
 */
#define OF_MATCH_MASK_SCTP_DST_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).sctp_dst))


/* Mask/value check/set macros for bsn_l3_src_class_id */

/**
 * Set the mask for an exact match of bsn_l3_src_class_id
 */
#define OF_MATCH_MASK_BSN_L3_SRC_CLASS_ID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_l3_src_class_id, 0xff, \
        sizeof(((_match)->masks).bsn_l3_src_class_id))

/**
 * Clear the mask for bsn_l3_src_class_id making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_L3_SRC_CLASS_ID_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_l3_src_class_id, 0, \
        sizeof(((_match)->masks).bsn_l3_src_class_id))

/**
 * Test whether the match is exact for bsn_l3_src_class_id
 */
#define OF_MATCH_MASK_BSN_L3_SRC_CLASS_ID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_l3_src_class_id))

/**
 * Test whether key bsn_l3_src_class_id is being checked in the match
 */
#define OF_MATCH_MASK_BSN_L3_SRC_CLASS_ID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_l3_src_class_id))


/* Mask/value check/set macros for bsn_ip_fragmentation */

/**
 * Set the mask for an exact match of bsn_ip_fragmentation
 */
#define OF_MATCH_MASK_BSN_IP_FRAGMENTATION_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_ip_fragmentation, 0xff, \
        sizeof(((_match)->masks).bsn_ip_fragmentation))

/**
 * Clear the mask for bsn_ip_fragmentation making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_IP_FRAGMENTATION_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_ip_fragmentation, 0, \
        sizeof(((_match)->masks).bsn_ip_fragmentation))

/**
 * Test whether the match is exact for bsn_ip_fragmentation
 */
#define OF_MATCH_MASK_BSN_IP_FRAGMENTATION_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_ip_fragmentation))

/**
 * Test whether key bsn_ip_fragmentation is being checked in the match
 */
#define OF_MATCH_MASK_BSN_IP_FRAGMENTATION_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_ip_fragmentation))


/* Mask/value check/set macros for ip_ecn */

/**
 * Set the mask for an exact match of ip_ecn
 */
#define OF_MATCH_MASK_IP_ECN_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ip_ecn, 0xff, \
        sizeof(((_match)->masks).ip_ecn))

/**
 * Clear the mask for ip_ecn making that field inactive for the match
 */
#define OF_MATCH_MASK_IP_ECN_CLEAR(_match) \
    MEMSET(&(_match)->masks.ip_ecn, 0, \
        sizeof(((_match)->masks).ip_ecn))

/**
 * Test whether the match is exact for ip_ecn
 */
#define OF_MATCH_MASK_IP_ECN_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ip_ecn))

/**
 * Test whether key ip_ecn is being checked in the match
 */
#define OF_MATCH_MASK_IP_ECN_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ip_ecn))


/* Mask/value check/set macros for bsn_udf1 */

/**
 * Set the mask for an exact match of bsn_udf1
 */
#define OF_MATCH_MASK_BSN_UDF1_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_udf1, 0xff, \
        sizeof(((_match)->masks).bsn_udf1))

/**
 * Clear the mask for bsn_udf1 making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_UDF1_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_udf1, 0, \
        sizeof(((_match)->masks).bsn_udf1))

/**
 * Test whether the match is exact for bsn_udf1
 */
#define OF_MATCH_MASK_BSN_UDF1_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_udf1))

/**
 * Test whether key bsn_udf1 is being checked in the match
 */
#define OF_MATCH_MASK_BSN_UDF1_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_udf1))


/* Mask/value check/set macros for bsn_udf0 */

/**
 * Set the mask for an exact match of bsn_udf0
 */
#define OF_MATCH_MASK_BSN_UDF0_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_udf0, 0xff, \
        sizeof(((_match)->masks).bsn_udf0))

/**
 * Clear the mask for bsn_udf0 making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_UDF0_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_udf0, 0, \
        sizeof(((_match)->masks).bsn_udf0))

/**
 * Test whether the match is exact for bsn_udf0
 */
#define OF_MATCH_MASK_BSN_UDF0_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_udf0))

/**
 * Test whether key bsn_udf0 is being checked in the match
 */
#define OF_MATCH_MASK_BSN_UDF0_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_udf0))


/* Mask/value check/set macros for encap_eth_dst */

/**
 * Set the mask for an exact match of encap_eth_dst
 */
#define OF_MATCH_MASK_ENCAP_ETH_DST_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.encap_eth_dst, 0xff, \
        sizeof(((_match)->masks).encap_eth_dst))

/**
 * Clear the mask for encap_eth_dst making that field inactive for the match
 */
#define OF_MATCH_MASK_ENCAP_ETH_DST_CLEAR(_match) \
    MEMSET(&(_match)->masks.encap_eth_dst, 0, \
        sizeof(((_match)->masks).encap_eth_dst))

/**
 * Test whether the match is exact for encap_eth_dst
 */
#define OF_MATCH_MASK_ENCAP_ETH_DST_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).encap_eth_dst))

/**
 * Test whether key encap_eth_dst is being checked in the match
 */
#define OF_MATCH_MASK_ENCAP_ETH_DST_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).encap_eth_dst))


/* Mask/value check/set macros for bsn_udf2 */

/**
 * Set the mask for an exact match of bsn_udf2
 */
#define OF_MATCH_MASK_BSN_UDF2_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_udf2, 0xff, \
        sizeof(((_match)->masks).bsn_udf2))

/**
 * Clear the mask for bsn_udf2 making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_UDF2_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_udf2, 0, \
        sizeof(((_match)->masks).bsn_udf2))

/**
 * Test whether the match is exact for bsn_udf2
 */
#define OF_MATCH_MASK_BSN_UDF2_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_udf2))

/**
 * Test whether key bsn_udf2 is being checked in the match
 */
#define OF_MATCH_MASK_BSN_UDF2_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_udf2))


/* Mask/value check/set macros for bsn_udf5 */

/**
 * Set the mask for an exact match of bsn_udf5
 */
#define OF_MATCH_MASK_BSN_UDF5_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_udf5, 0xff, \
        sizeof(((_match)->masks).bsn_udf5))

/**
 * Clear the mask for bsn_udf5 making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_UDF5_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_udf5, 0, \
        sizeof(((_match)->masks).bsn_udf5))

/**
 * Test whether the match is exact for bsn_udf5
 */
#define OF_MATCH_MASK_BSN_UDF5_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_udf5))

/**
 * Test whether key bsn_udf5 is being checked in the match
 */
#define OF_MATCH_MASK_BSN_UDF5_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_udf5))


/* Mask/value check/set macros for bsn_udf4 */

/**
 * Set the mask for an exact match of bsn_udf4
 */
#define OF_MATCH_MASK_BSN_UDF4_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_udf4, 0xff, \
        sizeof(((_match)->masks).bsn_udf4))

/**
 * Clear the mask for bsn_udf4 making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_UDF4_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_udf4, 0, \
        sizeof(((_match)->masks).bsn_udf4))

/**
 * Test whether the match is exact for bsn_udf4
 */
#define OF_MATCH_MASK_BSN_UDF4_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_udf4))

/**
 * Test whether key bsn_udf4 is being checked in the match
 */
#define OF_MATCH_MASK_BSN_UDF4_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_udf4))


/* Mask/value check/set macros for bsn_udf7 */

/**
 * Set the mask for an exact match of bsn_udf7
 */
#define OF_MATCH_MASK_BSN_UDF7_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_udf7, 0xff, \
        sizeof(((_match)->masks).bsn_udf7))

/**
 * Clear the mask for bsn_udf7 making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_UDF7_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_udf7, 0, \
        sizeof(((_match)->masks).bsn_udf7))

/**
 * Test whether the match is exact for bsn_udf7
 */
#define OF_MATCH_MASK_BSN_UDF7_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_udf7))

/**
 * Test whether key bsn_udf7 is being checked in the match
 */
#define OF_MATCH_MASK_BSN_UDF7_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_udf7))


/* Mask/value check/set macros for bsn_udf6 */

/**
 * Set the mask for an exact match of bsn_udf6
 */
#define OF_MATCH_MASK_BSN_UDF6_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_udf6, 0xff, \
        sizeof(((_match)->masks).bsn_udf6))

/**
 * Clear the mask for bsn_udf6 making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_UDF6_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_udf6, 0, \
        sizeof(((_match)->masks).bsn_udf6))

/**
 * Test whether the match is exact for bsn_udf6
 */
#define OF_MATCH_MASK_BSN_UDF6_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_udf6))

/**
 * Test whether key bsn_udf6 is being checked in the match
 */
#define OF_MATCH_MASK_BSN_UDF6_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_udf6))


/* Mask/value check/set macros for bsn_udf3 */

/**
 * Set the mask for an exact match of bsn_udf3
 */
#define OF_MATCH_MASK_BSN_UDF3_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_udf3, 0xff, \
        sizeof(((_match)->masks).bsn_udf3))

/**
 * Clear the mask for bsn_udf3 making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_UDF3_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_udf3, 0, \
        sizeof(((_match)->masks).bsn_udf3))

/**
 * Test whether the match is exact for bsn_udf3
 */
#define OF_MATCH_MASK_BSN_UDF3_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_udf3))

/**
 * Test whether key bsn_udf3 is being checked in the match
 */
#define OF_MATCH_MASK_BSN_UDF3_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_udf3))


/* Mask/value check/set macros for bsn_inner_eth_src */

/**
 * Set the mask for an exact match of bsn_inner_eth_src
 */
#define OF_MATCH_MASK_BSN_INNER_ETH_SRC_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_inner_eth_src, 0xff, \
        sizeof(((_match)->masks).bsn_inner_eth_src))

/**
 * Clear the mask for bsn_inner_eth_src making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_INNER_ETH_SRC_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_inner_eth_src, 0, \
        sizeof(((_match)->masks).bsn_inner_eth_src))

/**
 * Test whether the match is exact for bsn_inner_eth_src
 */
#define OF_MATCH_MASK_BSN_INNER_ETH_SRC_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_inner_eth_src))

/**
 * Test whether key bsn_inner_eth_src is being checked in the match
 */
#define OF_MATCH_MASK_BSN_INNER_ETH_SRC_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_inner_eth_src))


/* Mask/value check/set macros for nsi */

/**
 * Set the mask for an exact match of nsi
 */
#define OF_MATCH_MASK_NSI_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.nsi, 0xff, \
        sizeof(((_match)->masks).nsi))

/**
 * Clear the mask for nsi making that field inactive for the match
 */
#define OF_MATCH_MASK_NSI_CLEAR(_match) \
    MEMSET(&(_match)->masks.nsi, 0, \
        sizeof(((_match)->masks).nsi))

/**
 * Test whether the match is exact for nsi
 */
#define OF_MATCH_MASK_NSI_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).nsi))

/**
 * Test whether key nsi is being checked in the match
 */
#define OF_MATCH_MASK_NSI_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).nsi))


/* Mask/value check/set macros for bsn_lag_id */

/**
 * Set the mask for an exact match of bsn_lag_id
 */
#define OF_MATCH_MASK_BSN_LAG_ID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_lag_id, 0xff, \
        sizeof(((_match)->masks).bsn_lag_id))

/**
 * Clear the mask for bsn_lag_id making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_LAG_ID_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_lag_id, 0, \
        sizeof(((_match)->masks).bsn_lag_id))

/**
 * Test whether the match is exact for bsn_lag_id
 */
#define OF_MATCH_MASK_BSN_LAG_ID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_lag_id))

/**
 * Test whether key bsn_lag_id is being checked in the match
 */
#define OF_MATCH_MASK_BSN_LAG_ID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_lag_id))


/* Mask/value check/set macros for vlan_pcp */

/**
 * Set the mask for an exact match of vlan_pcp
 */
#define OF_MATCH_MASK_VLAN_PCP_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.vlan_pcp, 0xff, \
        sizeof(((_match)->masks).vlan_pcp))

/**
 * Clear the mask for vlan_pcp making that field inactive for the match
 */
#define OF_MATCH_MASK_VLAN_PCP_CLEAR(_match) \
    MEMSET(&(_match)->masks.vlan_pcp, 0, \
        sizeof(((_match)->masks).vlan_pcp))

/**
 * Test whether the match is exact for vlan_pcp
 */
#define OF_MATCH_MASK_VLAN_PCP_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).vlan_pcp))

/**
 * Test whether key vlan_pcp is being checked in the match
 */
#define OF_MATCH_MASK_VLAN_PCP_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).vlan_pcp))


/* Mask/value check/set macros for ipv4_src */

/**
 * Set the mask for an exact match of ipv4_src
 */
#define OF_MATCH_MASK_IPV4_SRC_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ipv4_src, 0xff, \
        sizeof(((_match)->masks).ipv4_src))

/**
 * Clear the mask for ipv4_src making that field inactive for the match
 */
#define OF_MATCH_MASK_IPV4_SRC_CLEAR(_match) \
    MEMSET(&(_match)->masks.ipv4_src, 0, \
        sizeof(((_match)->masks).ipv4_src))

/**
 * Test whether the match is exact for ipv4_src
 */
#define OF_MATCH_MASK_IPV4_SRC_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ipv4_src))

/**
 * Test whether key ipv4_src is being checked in the match
 */
#define OF_MATCH_MASK_IPV4_SRC_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ipv4_src))


/* Mask/value check/set macros for ipv6_dst */

/**
 * Set the mask for an exact match of ipv6_dst
 */
#define OF_MATCH_MASK_IPV6_DST_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ipv6_dst, 0xff, \
        sizeof(((_match)->masks).ipv6_dst))

/**
 * Clear the mask for ipv6_dst making that field inactive for the match
 */
#define OF_MATCH_MASK_IPV6_DST_CLEAR(_match) \
    MEMSET(&(_match)->masks.ipv6_dst, 0, \
        sizeof(((_match)->masks).ipv6_dst))

/**
 * Test whether the match is exact for ipv6_dst
 */
#define OF_MATCH_MASK_IPV6_DST_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ipv6_dst))

/**
 * Test whether key ipv6_dst is being checked in the match
 */
#define OF_MATCH_MASK_IPV6_DST_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ipv6_dst))


/* Mask/value check/set macros for nsp */

/**
 * Set the mask for an exact match of nsp
 */
#define OF_MATCH_MASK_NSP_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.nsp, 0xff, \
        sizeof(((_match)->masks).nsp))

/**
 * Clear the mask for nsp making that field inactive for the match
 */
#define OF_MATCH_MASK_NSP_CLEAR(_match) \
    MEMSET(&(_match)->masks.nsp, 0, \
        sizeof(((_match)->masks).nsp))

/**
 * Test whether the match is exact for nsp
 */
#define OF_MATCH_MASK_NSP_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).nsp))

/**
 * Test whether key nsp is being checked in the match
 */
#define OF_MATCH_MASK_NSP_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).nsp))


/* Mask/value check/set macros for eth_src */

/**
 * Set the mask for an exact match of eth_src
 */
#define OF_MATCH_MASK_ETH_SRC_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.eth_src, 0xff, \
        sizeof(((_match)->masks).eth_src))

/**
 * Clear the mask for eth_src making that field inactive for the match
 */
#define OF_MATCH_MASK_ETH_SRC_CLEAR(_match) \
    MEMSET(&(_match)->masks.eth_src, 0, \
        sizeof(((_match)->masks).eth_src))

/**
 * Test whether the match is exact for eth_src
 */
#define OF_MATCH_MASK_ETH_SRC_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).eth_src))

/**
 * Test whether key eth_src is being checked in the match
 */
#define OF_MATCH_MASK_ETH_SRC_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).eth_src))


/* Mask/value check/set macros for icmpv6_code */

/**
 * Set the mask for an exact match of icmpv6_code
 */
#define OF_MATCH_MASK_ICMPV6_CODE_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.icmpv6_code, 0xff, \
        sizeof(((_match)->masks).icmpv6_code))

/**
 * Clear the mask for icmpv6_code making that field inactive for the match
 */
#define OF_MATCH_MASK_ICMPV6_CODE_CLEAR(_match) \
    MEMSET(&(_match)->masks.icmpv6_code, 0, \
        sizeof(((_match)->masks).icmpv6_code))

/**
 * Test whether the match is exact for icmpv6_code
 */
#define OF_MATCH_MASK_ICMPV6_CODE_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).icmpv6_code))

/**
 * Test whether key icmpv6_code is being checked in the match
 */
#define OF_MATCH_MASK_ICMPV6_CODE_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).icmpv6_code))


/* Mask/value check/set macros for bsn_in_ports_512 */

/**
 * Set the mask for an exact match of bsn_in_ports_512
 */
#define OF_MATCH_MASK_BSN_IN_PORTS_512_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_in_ports_512, 0xff, \
        sizeof(((_match)->masks).bsn_in_ports_512))

/**
 * Clear the mask for bsn_in_ports_512 making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_IN_PORTS_512_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_in_ports_512, 0, \
        sizeof(((_match)->masks).bsn_in_ports_512))

/**
 * Test whether the match is exact for bsn_in_ports_512
 */
#define OF_MATCH_MASK_BSN_IN_PORTS_512_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_in_ports_512))

/**
 * Test whether key bsn_in_ports_512 is being checked in the match
 */
#define OF_MATCH_MASK_BSN_IN_PORTS_512_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_in_ports_512))


/* Mask/value check/set macros for udp_src */

/**
 * Set the mask for an exact match of udp_src
 */
#define OF_MATCH_MASK_UDP_SRC_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.udp_src, 0xff, \
        sizeof(((_match)->masks).udp_src))

/**
 * Clear the mask for udp_src making that field inactive for the match
 */
#define OF_MATCH_MASK_UDP_SRC_CLEAR(_match) \
    MEMSET(&(_match)->masks.udp_src, 0, \
        sizeof(((_match)->masks).udp_src))

/**
 * Test whether the match is exact for udp_src
 */
#define OF_MATCH_MASK_UDP_SRC_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).udp_src))

/**
 * Test whether key udp_src is being checked in the match
 */
#define OF_MATCH_MASK_UDP_SRC_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).udp_src))


/* Mask/value check/set macros for in_port */

/**
 * Set the mask for an exact match of in_port
 */
#define OF_MATCH_MASK_IN_PORT_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.in_port, 0xff, \
        sizeof(((_match)->masks).in_port))

/**
 * Clear the mask for in_port making that field inactive for the match
 */
#define OF_MATCH_MASK_IN_PORT_CLEAR(_match) \
    MEMSET(&(_match)->masks.in_port, 0, \
        sizeof(((_match)->masks).in_port))

/**
 * Test whether the match is exact for in_port
 */
#define OF_MATCH_MASK_IN_PORT_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).in_port))

/**
 * Test whether key in_port is being checked in the match
 */
#define OF_MATCH_MASK_IN_PORT_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).in_port))


/* Mask/value check/set macros for bsn_l3_interface_class_id */

/**
 * Set the mask for an exact match of bsn_l3_interface_class_id
 */
#define OF_MATCH_MASK_BSN_L3_INTERFACE_CLASS_ID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_l3_interface_class_id, 0xff, \
        sizeof(((_match)->masks).bsn_l3_interface_class_id))

/**
 * Clear the mask for bsn_l3_interface_class_id making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_L3_INTERFACE_CLASS_ID_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_l3_interface_class_id, 0, \
        sizeof(((_match)->masks).bsn_l3_interface_class_id))

/**
 * Test whether the match is exact for bsn_l3_interface_class_id
 */
#define OF_MATCH_MASK_BSN_L3_INTERFACE_CLASS_ID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_l3_interface_class_id))

/**
 * Test whether key bsn_l3_interface_class_id is being checked in the match
 */
#define OF_MATCH_MASK_BSN_L3_INTERFACE_CLASS_ID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_l3_interface_class_id))


/* Mask/value check/set macros for ofdpa_mpls_l2_port */

/**
 * Set the mask for an exact match of ofdpa_mpls_l2_port
 */
#define OF_MATCH_MASK_OFDPA_MPLS_L2_PORT_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ofdpa_mpls_l2_port, 0xff, \
        sizeof(((_match)->masks).ofdpa_mpls_l2_port))

/**
 * Clear the mask for ofdpa_mpls_l2_port making that field inactive for the match
 */
#define OF_MATCH_MASK_OFDPA_MPLS_L2_PORT_CLEAR(_match) \
    MEMSET(&(_match)->masks.ofdpa_mpls_l2_port, 0, \
        sizeof(((_match)->masks).ofdpa_mpls_l2_port))

/**
 * Test whether the match is exact for ofdpa_mpls_l2_port
 */
#define OF_MATCH_MASK_OFDPA_MPLS_L2_PORT_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ofdpa_mpls_l2_port))

/**
 * Test whether key ofdpa_mpls_l2_port is being checked in the match
 */
#define OF_MATCH_MASK_OFDPA_MPLS_L2_PORT_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ofdpa_mpls_l2_port))


/* Mask/value check/set macros for encap_eth_type */

/**
 * Set the mask for an exact match of encap_eth_type
 */
#define OF_MATCH_MASK_ENCAP_ETH_TYPE_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.encap_eth_type, 0xff, \
        sizeof(((_match)->masks).encap_eth_type))

/**
 * Clear the mask for encap_eth_type making that field inactive for the match
 */
#define OF_MATCH_MASK_ENCAP_ETH_TYPE_CLEAR(_match) \
    MEMSET(&(_match)->masks.encap_eth_type, 0, \
        sizeof(((_match)->masks).encap_eth_type))

/**
 * Test whether the match is exact for encap_eth_type
 */
#define OF_MATCH_MASK_ENCAP_ETH_TYPE_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).encap_eth_type))

/**
 * Test whether key encap_eth_type is being checked in the match
 */
#define OF_MATCH_MASK_ENCAP_ETH_TYPE_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).encap_eth_type))


/* Mask/value check/set macros for tcp_src */

/**
 * Set the mask for an exact match of tcp_src
 */
#define OF_MATCH_MASK_TCP_SRC_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.tcp_src, 0xff, \
        sizeof(((_match)->masks).tcp_src))

/**
 * Clear the mask for tcp_src making that field inactive for the match
 */
#define OF_MATCH_MASK_TCP_SRC_CLEAR(_match) \
    MEMSET(&(_match)->masks.tcp_src, 0, \
        sizeof(((_match)->masks).tcp_src))

/**
 * Test whether the match is exact for tcp_src
 */
#define OF_MATCH_MASK_TCP_SRC_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).tcp_src))

/**
 * Test whether key tcp_src is being checked in the match
 */
#define OF_MATCH_MASK_TCP_SRC_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).tcp_src))


/* Mask/value check/set macros for circuit_och_sigtype_basic */

/**
 * Set the mask for an exact match of circuit_och_sigtype_basic
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGTYPE_BASIC_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.circuit_och_sigtype_basic, 0xff, \
        sizeof(((_match)->masks).circuit_och_sigtype_basic))

/**
 * Clear the mask for circuit_och_sigtype_basic making that field inactive for the match
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGTYPE_BASIC_CLEAR(_match) \
    MEMSET(&(_match)->masks.circuit_och_sigtype_basic, 0, \
        sizeof(((_match)->masks).circuit_och_sigtype_basic))

/**
 * Test whether the match is exact for circuit_och_sigtype_basic
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGTYPE_BASIC_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).circuit_och_sigtype_basic))

/**
 * Test whether key circuit_och_sigtype_basic is being checked in the match
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGTYPE_BASIC_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).circuit_och_sigtype_basic))


/* Mask/value check/set macros for och_sigatt_basic */

/**
 * Set the mask for an exact match of och_sigatt_basic
 */
#define OF_MATCH_MASK_OCH_SIGATT_BASIC_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.och_sigatt_basic, 0xff, \
        sizeof(((_match)->masks).och_sigatt_basic))

/**
 * Clear the mask for och_sigatt_basic making that field inactive for the match
 */
#define OF_MATCH_MASK_OCH_SIGATT_BASIC_CLEAR(_match) \
    MEMSET(&(_match)->masks.och_sigatt_basic, 0, \
        sizeof(((_match)->masks).och_sigatt_basic))

/**
 * Test whether the match is exact for och_sigatt_basic
 */
#define OF_MATCH_MASK_OCH_SIGATT_BASIC_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).och_sigatt_basic))

/**
 * Test whether key och_sigatt_basic is being checked in the match
 */
#define OF_MATCH_MASK_OCH_SIGATT_BASIC_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).och_sigatt_basic))


/* Mask/value check/set macros for sctp_src */

/**
 * Set the mask for an exact match of sctp_src
 */
#define OF_MATCH_MASK_SCTP_SRC_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.sctp_src, 0xff, \
        sizeof(((_match)->masks).sctp_src))

/**
 * Clear the mask for sctp_src making that field inactive for the match
 */
#define OF_MATCH_MASK_SCTP_SRC_CLEAR(_match) \
    MEMSET(&(_match)->masks.sctp_src, 0, \
        sizeof(((_match)->masks).sctp_src))

/**
 * Test whether the match is exact for sctp_src
 */
#define OF_MATCH_MASK_SCTP_SRC_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).sctp_src))

/**
 * Test whether key sctp_src is being checked in the match
 */
#define OF_MATCH_MASK_SCTP_SRC_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).sctp_src))


/* Mask/value check/set macros for bsn_vlan_xlate_port_group_id */

/**
 * Set the mask for an exact match of bsn_vlan_xlate_port_group_id
 */
#define OF_MATCH_MASK_BSN_VLAN_XLATE_PORT_GROUP_ID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_vlan_xlate_port_group_id, 0xff, \
        sizeof(((_match)->masks).bsn_vlan_xlate_port_group_id))

/**
 * Clear the mask for bsn_vlan_xlate_port_group_id making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_VLAN_XLATE_PORT_GROUP_ID_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_vlan_xlate_port_group_id, 0, \
        sizeof(((_match)->masks).bsn_vlan_xlate_port_group_id))

/**
 * Test whether the match is exact for bsn_vlan_xlate_port_group_id
 */
#define OF_MATCH_MASK_BSN_VLAN_XLATE_PORT_GROUP_ID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_vlan_xlate_port_group_id))

/**
 * Test whether key bsn_vlan_xlate_port_group_id is being checked in the match
 */
#define OF_MATCH_MASK_BSN_VLAN_XLATE_PORT_GROUP_ID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_vlan_xlate_port_group_id))


/* Mask/value check/set macros for circuit_och_sigtype */

/**
 * Set the mask for an exact match of circuit_och_sigtype
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGTYPE_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.circuit_och_sigtype, 0xff, \
        sizeof(((_match)->masks).circuit_och_sigtype))

/**
 * Clear the mask for circuit_och_sigtype making that field inactive for the match
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGTYPE_CLEAR(_match) \
    MEMSET(&(_match)->masks.circuit_och_sigtype, 0, \
        sizeof(((_match)->masks).circuit_och_sigtype))

/**
 * Test whether the match is exact for circuit_och_sigtype
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGTYPE_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).circuit_och_sigtype))

/**
 * Test whether key circuit_och_sigtype is being checked in the match
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGTYPE_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).circuit_och_sigtype))


/* Mask/value check/set macros for bsn_l2_cache_hit */

/**
 * Set the mask for an exact match of bsn_l2_cache_hit
 */
#define OF_MATCH_MASK_BSN_L2_CACHE_HIT_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_l2_cache_hit, 0xff, \
        sizeof(((_match)->masks).bsn_l2_cache_hit))

/**
 * Clear the mask for bsn_l2_cache_hit making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_L2_CACHE_HIT_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_l2_cache_hit, 0, \
        sizeof(((_match)->masks).bsn_l2_cache_hit))

/**
 * Test whether the match is exact for bsn_l2_cache_hit
 */
#define OF_MATCH_MASK_BSN_L2_CACHE_HIT_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_l2_cache_hit))

/**
 * Test whether key bsn_l2_cache_hit is being checked in the match
 */
#define OF_MATCH_MASK_BSN_L2_CACHE_HIT_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_l2_cache_hit))


/* Mask/value check/set macros for mpls_label */

/**
 * Set the mask for an exact match of mpls_label
 */
#define OF_MATCH_MASK_MPLS_LABEL_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.mpls_label, 0xff, \
        sizeof(((_match)->masks).mpls_label))

/**
 * Clear the mask for mpls_label making that field inactive for the match
 */
#define OF_MATCH_MASK_MPLS_LABEL_CLEAR(_match) \
    MEMSET(&(_match)->masks.mpls_label, 0, \
        sizeof(((_match)->masks).mpls_label))

/**
 * Test whether the match is exact for mpls_label
 */
#define OF_MATCH_MASK_MPLS_LABEL_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).mpls_label))

/**
 * Test whether key mpls_label is being checked in the match
 */
#define OF_MATCH_MASK_MPLS_LABEL_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).mpls_label))


/* Mask/value check/set macros for ofdpa_ovid */

/**
 * Set the mask for an exact match of ofdpa_ovid
 */
#define OF_MATCH_MASK_OFDPA_OVID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ofdpa_ovid, 0xff, \
        sizeof(((_match)->masks).ofdpa_ovid))

/**
 * Clear the mask for ofdpa_ovid making that field inactive for the match
 */
#define OF_MATCH_MASK_OFDPA_OVID_CLEAR(_match) \
    MEMSET(&(_match)->masks.ofdpa_ovid, 0, \
        sizeof(((_match)->masks).ofdpa_ovid))

/**
 * Test whether the match is exact for ofdpa_ovid
 */
#define OF_MATCH_MASK_OFDPA_OVID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ofdpa_ovid))

/**
 * Test whether key ofdpa_ovid is being checked in the match
 */
#define OF_MATCH_MASK_OFDPA_OVID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ofdpa_ovid))


/* Mask/value check/set macros for tun_gpe_np */

/**
 * Set the mask for an exact match of tun_gpe_np
 */
#define OF_MATCH_MASK_TUN_GPE_NP_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.tun_gpe_np, 0xff, \
        sizeof(((_match)->masks).tun_gpe_np))

/**
 * Clear the mask for tun_gpe_np making that field inactive for the match
 */
#define OF_MATCH_MASK_TUN_GPE_NP_CLEAR(_match) \
    MEMSET(&(_match)->masks.tun_gpe_np, 0, \
        sizeof(((_match)->masks).tun_gpe_np))

/**
 * Test whether the match is exact for tun_gpe_np
 */
#define OF_MATCH_MASK_TUN_GPE_NP_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).tun_gpe_np))

/**
 * Test whether key tun_gpe_np is being checked in the match
 */
#define OF_MATCH_MASK_TUN_GPE_NP_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).tun_gpe_np))


/* Mask/value check/set macros for ovs_tcp_flags */

/**
 * Set the mask for an exact match of ovs_tcp_flags
 */
#define OF_MATCH_MASK_OVS_TCP_FLAGS_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ovs_tcp_flags, 0xff, \
        sizeof(((_match)->masks).ovs_tcp_flags))

/**
 * Clear the mask for ovs_tcp_flags making that field inactive for the match
 */
#define OF_MATCH_MASK_OVS_TCP_FLAGS_CLEAR(_match) \
    MEMSET(&(_match)->masks.ovs_tcp_flags, 0, \
        sizeof(((_match)->masks).ovs_tcp_flags))

/**
 * Test whether the match is exact for ovs_tcp_flags
 */
#define OF_MATCH_MASK_OVS_TCP_FLAGS_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ovs_tcp_flags))

/**
 * Test whether key ovs_tcp_flags is being checked in the match
 */
#define OF_MATCH_MASK_OVS_TCP_FLAGS_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ovs_tcp_flags))


/* Mask/value check/set macros for encap_eth_src */

/**
 * Set the mask for an exact match of encap_eth_src
 */
#define OF_MATCH_MASK_ENCAP_ETH_SRC_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.encap_eth_src, 0xff, \
        sizeof(((_match)->masks).encap_eth_src))

/**
 * Clear the mask for encap_eth_src making that field inactive for the match
 */
#define OF_MATCH_MASK_ENCAP_ETH_SRC_CLEAR(_match) \
    MEMSET(&(_match)->masks.encap_eth_src, 0, \
        sizeof(((_match)->masks).encap_eth_src))

/**
 * Test whether the match is exact for encap_eth_src
 */
#define OF_MATCH_MASK_ENCAP_ETH_SRC_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).encap_eth_src))

/**
 * Test whether key encap_eth_src is being checked in the match
 */
#define OF_MATCH_MASK_ENCAP_ETH_SRC_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).encap_eth_src))


/* Mask/value check/set macros for circuit_och_sigid_basic */

/**
 * Set the mask for an exact match of circuit_och_sigid_basic
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGID_BASIC_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.circuit_och_sigid_basic, 0xff, \
        sizeof(((_match)->masks).circuit_och_sigid_basic))

/**
 * Clear the mask for circuit_och_sigid_basic making that field inactive for the match
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGID_BASIC_CLEAR(_match) \
    MEMSET(&(_match)->masks.circuit_och_sigid_basic, 0, \
        sizeof(((_match)->masks).circuit_och_sigid_basic))

/**
 * Test whether the match is exact for circuit_och_sigid_basic
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGID_BASIC_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).circuit_och_sigid_basic))

/**
 * Test whether key circuit_och_sigid_basic is being checked in the match
 */
#define OF_MATCH_MASK_CIRCUIT_OCH_SIGID_BASIC_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).circuit_och_sigid_basic))


/* Mask/value check/set macros for bsn_inner_eth_dst */

/**
 * Set the mask for an exact match of bsn_inner_eth_dst
 */
#define OF_MATCH_MASK_BSN_INNER_ETH_DST_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_inner_eth_dst, 0xff, \
        sizeof(((_match)->masks).bsn_inner_eth_dst))

/**
 * Clear the mask for bsn_inner_eth_dst making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_INNER_ETH_DST_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_inner_eth_dst, 0, \
        sizeof(((_match)->masks).bsn_inner_eth_dst))

/**
 * Test whether the match is exact for bsn_inner_eth_dst
 */
#define OF_MATCH_MASK_BSN_INNER_ETH_DST_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_inner_eth_dst))

/**
 * Test whether key bsn_inner_eth_dst is being checked in the match
 */
#define OF_MATCH_MASK_BSN_INNER_ETH_DST_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_inner_eth_dst))


/* Mask/value check/set macros for icmpv6_type */

/**
 * Set the mask for an exact match of icmpv6_type
 */
#define OF_MATCH_MASK_ICMPV6_TYPE_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.icmpv6_type, 0xff, \
        sizeof(((_match)->masks).icmpv6_type))

/**
 * Clear the mask for icmpv6_type making that field inactive for the match
 */
#define OF_MATCH_MASK_ICMPV6_TYPE_CLEAR(_match) \
    MEMSET(&(_match)->masks.icmpv6_type, 0, \
        sizeof(((_match)->masks).icmpv6_type))

/**
 * Test whether the match is exact for icmpv6_type
 */
#define OF_MATCH_MASK_ICMPV6_TYPE_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).icmpv6_type))

/**
 * Test whether key icmpv6_type is being checked in the match
 */
#define OF_MATCH_MASK_ICMPV6_TYPE_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).icmpv6_type))


/* Mask/value check/set macros for bsn_in_ports_128 */

/**
 * Set the mask for an exact match of bsn_in_ports_128
 */
#define OF_MATCH_MASK_BSN_IN_PORTS_128_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_in_ports_128, 0xff, \
        sizeof(((_match)->masks).bsn_in_ports_128))

/**
 * Clear the mask for bsn_in_ports_128 making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_IN_PORTS_128_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_in_ports_128, 0, \
        sizeof(((_match)->masks).bsn_in_ports_128))

/**
 * Test whether the match is exact for bsn_in_ports_128
 */
#define OF_MATCH_MASK_BSN_IN_PORTS_128_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_in_ports_128))

/**
 * Test whether key bsn_in_ports_128 is being checked in the match
 */
#define OF_MATCH_MASK_BSN_IN_PORTS_128_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_in_ports_128))


/* Mask/value check/set macros for pbb_uca */

/**
 * Set the mask for an exact match of pbb_uca
 */
#define OF_MATCH_MASK_PBB_UCA_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.pbb_uca, 0xff, \
        sizeof(((_match)->masks).pbb_uca))

/**
 * Clear the mask for pbb_uca making that field inactive for the match
 */
#define OF_MATCH_MASK_PBB_UCA_CLEAR(_match) \
    MEMSET(&(_match)->masks.pbb_uca, 0, \
        sizeof(((_match)->masks).pbb_uca))

/**
 * Test whether the match is exact for pbb_uca
 */
#define OF_MATCH_MASK_PBB_UCA_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).pbb_uca))

/**
 * Test whether key pbb_uca is being checked in the match
 */
#define OF_MATCH_MASK_PBB_UCA_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).pbb_uca))


/* Mask/value check/set macros for mpls_tc */

/**
 * Set the mask for an exact match of mpls_tc
 */
#define OF_MATCH_MASK_MPLS_TC_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.mpls_tc, 0xff, \
        sizeof(((_match)->masks).mpls_tc))

/**
 * Clear the mask for mpls_tc making that field inactive for the match
 */
#define OF_MATCH_MASK_MPLS_TC_CLEAR(_match) \
    MEMSET(&(_match)->masks.mpls_tc, 0, \
        sizeof(((_match)->masks).mpls_tc))

/**
 * Test whether the match is exact for mpls_tc
 */
#define OF_MATCH_MASK_MPLS_TC_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).mpls_tc))

/**
 * Test whether key mpls_tc is being checked in the match
 */
#define OF_MATCH_MASK_MPLS_TC_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).mpls_tc))


/* Mask/value check/set macros for ipv6_nd_tll */

/**
 * Set the mask for an exact match of ipv6_nd_tll
 */
#define OF_MATCH_MASK_IPV6_ND_TLL_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ipv6_nd_tll, 0xff, \
        sizeof(((_match)->masks).ipv6_nd_tll))

/**
 * Clear the mask for ipv6_nd_tll making that field inactive for the match
 */
#define OF_MATCH_MASK_IPV6_ND_TLL_CLEAR(_match) \
    MEMSET(&(_match)->masks.ipv6_nd_tll, 0, \
        sizeof(((_match)->masks).ipv6_nd_tll))

/**
 * Test whether the match is exact for ipv6_nd_tll
 */
#define OF_MATCH_MASK_IPV6_ND_TLL_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ipv6_nd_tll))

/**
 * Test whether key ipv6_nd_tll is being checked in the match
 */
#define OF_MATCH_MASK_IPV6_ND_TLL_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ipv6_nd_tll))


/* Mask/value check/set macros for nsh_mdtype */

/**
 * Set the mask for an exact match of nsh_mdtype
 */
#define OF_MATCH_MASK_NSH_MDTYPE_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.nsh_mdtype, 0xff, \
        sizeof(((_match)->masks).nsh_mdtype))

/**
 * Clear the mask for nsh_mdtype making that field inactive for the match
 */
#define OF_MATCH_MASK_NSH_MDTYPE_CLEAR(_match) \
    MEMSET(&(_match)->masks.nsh_mdtype, 0, \
        sizeof(((_match)->masks).nsh_mdtype))

/**
 * Test whether the match is exact for nsh_mdtype
 */
#define OF_MATCH_MASK_NSH_MDTYPE_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).nsh_mdtype))

/**
 * Test whether key nsh_mdtype is being checked in the match
 */
#define OF_MATCH_MASK_NSH_MDTYPE_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).nsh_mdtype))


/* Mask/value check/set macros for icmpv4_code */

/**
 * Set the mask for an exact match of icmpv4_code
 */
#define OF_MATCH_MASK_ICMPV4_CODE_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.icmpv4_code, 0xff, \
        sizeof(((_match)->masks).icmpv4_code))

/**
 * Clear the mask for icmpv4_code making that field inactive for the match
 */
#define OF_MATCH_MASK_ICMPV4_CODE_CLEAR(_match) \
    MEMSET(&(_match)->masks.icmpv4_code, 0, \
        sizeof(((_match)->masks).icmpv4_code))

/**
 * Test whether the match is exact for icmpv4_code
 */
#define OF_MATCH_MASK_ICMPV4_CODE_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).icmpv4_code))

/**
 * Test whether key icmpv4_code is being checked in the match
 */
#define OF_MATCH_MASK_ICMPV4_CODE_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).icmpv4_code))


/* Mask/value check/set macros for bsn_vrf */

/**
 * Set the mask for an exact match of bsn_vrf
 */
#define OF_MATCH_MASK_BSN_VRF_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_vrf, 0xff, \
        sizeof(((_match)->masks).bsn_vrf))

/**
 * Clear the mask for bsn_vrf making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_VRF_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_vrf, 0, \
        sizeof(((_match)->masks).bsn_vrf))

/**
 * Test whether the match is exact for bsn_vrf
 */
#define OF_MATCH_MASK_BSN_VRF_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_vrf))

/**
 * Test whether key bsn_vrf is being checked in the match
 */
#define OF_MATCH_MASK_BSN_VRF_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_vrf))


/* Mask/value check/set macros for bsn_inner_vlan_vid */

/**
 * Set the mask for an exact match of bsn_inner_vlan_vid
 */
#define OF_MATCH_MASK_BSN_INNER_VLAN_VID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_inner_vlan_vid, 0xff, \
        sizeof(((_match)->masks).bsn_inner_vlan_vid))

/**
 * Clear the mask for bsn_inner_vlan_vid making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_INNER_VLAN_VID_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_inner_vlan_vid, 0, \
        sizeof(((_match)->masks).bsn_inner_vlan_vid))

/**
 * Test whether the match is exact for bsn_inner_vlan_vid
 */
#define OF_MATCH_MASK_BSN_INNER_VLAN_VID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_inner_vlan_vid))

/**
 * Test whether key bsn_inner_vlan_vid is being checked in the match
 */
#define OF_MATCH_MASK_BSN_INNER_VLAN_VID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_inner_vlan_vid))


/* Mask/value check/set macros for tunnel_ipv4_src */

/**
 * Set the mask for an exact match of tunnel_ipv4_src
 */
#define OF_MATCH_MASK_TUNNEL_IPV4_SRC_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.tunnel_ipv4_src, 0xff, \
        sizeof(((_match)->masks).tunnel_ipv4_src))

/**
 * Clear the mask for tunnel_ipv4_src making that field inactive for the match
 */
#define OF_MATCH_MASK_TUNNEL_IPV4_SRC_CLEAR(_match) \
    MEMSET(&(_match)->masks.tunnel_ipv4_src, 0, \
        sizeof(((_match)->masks).tunnel_ipv4_src))

/**
 * Test whether the match is exact for tunnel_ipv4_src
 */
#define OF_MATCH_MASK_TUNNEL_IPV4_SRC_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).tunnel_ipv4_src))

/**
 * Test whether key tunnel_ipv4_src is being checked in the match
 */
#define OF_MATCH_MASK_TUNNEL_IPV4_SRC_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).tunnel_ipv4_src))


/* Mask/value check/set macros for bsn_global_vrf_allowed */

/**
 * Set the mask for an exact match of bsn_global_vrf_allowed
 */
#define OF_MATCH_MASK_BSN_GLOBAL_VRF_ALLOWED_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_global_vrf_allowed, 0xff, \
        sizeof(((_match)->masks).bsn_global_vrf_allowed))

/**
 * Clear the mask for bsn_global_vrf_allowed making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_GLOBAL_VRF_ALLOWED_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_global_vrf_allowed, 0, \
        sizeof(((_match)->masks).bsn_global_vrf_allowed))

/**
 * Test whether the match is exact for bsn_global_vrf_allowed
 */
#define OF_MATCH_MASK_BSN_GLOBAL_VRF_ALLOWED_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_global_vrf_allowed))

/**
 * Test whether key bsn_global_vrf_allowed is being checked in the match
 */
#define OF_MATCH_MASK_BSN_GLOBAL_VRF_ALLOWED_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_global_vrf_allowed))


/* Mask/value check/set macros for tun_gbp_flags */

/**
 * Set the mask for an exact match of tun_gbp_flags
 */
#define OF_MATCH_MASK_TUN_GBP_FLAGS_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.tun_gbp_flags, 0xff, \
        sizeof(((_match)->masks).tun_gbp_flags))

/**
 * Clear the mask for tun_gbp_flags making that field inactive for the match
 */
#define OF_MATCH_MASK_TUN_GBP_FLAGS_CLEAR(_match) \
    MEMSET(&(_match)->masks.tun_gbp_flags, 0, \
        sizeof(((_match)->masks).tun_gbp_flags))

/**
 * Test whether the match is exact for tun_gbp_flags
 */
#define OF_MATCH_MASK_TUN_GBP_FLAGS_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).tun_gbp_flags))

/**
 * Test whether key tun_gbp_flags is being checked in the match
 */
#define OF_MATCH_MASK_TUN_GBP_FLAGS_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).tun_gbp_flags))


/* Mask/value check/set macros for in_phy_port */

/**
 * Set the mask for an exact match of in_phy_port
 */
#define OF_MATCH_MASK_IN_PHY_PORT_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.in_phy_port, 0xff, \
        sizeof(((_match)->masks).in_phy_port))

/**
 * Clear the mask for in_phy_port making that field inactive for the match
 */
#define OF_MATCH_MASK_IN_PHY_PORT_CLEAR(_match) \
    MEMSET(&(_match)->masks.in_phy_port, 0, \
        sizeof(((_match)->masks).in_phy_port))

/**
 * Test whether the match is exact for in_phy_port
 */
#define OF_MATCH_MASK_IN_PHY_PORT_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).in_phy_port))

/**
 * Test whether key in_phy_port is being checked in the match
 */
#define OF_MATCH_MASK_IN_PHY_PORT_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).in_phy_port))


/* Mask/value check/set macros for bsn_vxlan_network_id */

/**
 * Set the mask for an exact match of bsn_vxlan_network_id
 */
#define OF_MATCH_MASK_BSN_VXLAN_NETWORK_ID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_vxlan_network_id, 0xff, \
        sizeof(((_match)->masks).bsn_vxlan_network_id))

/**
 * Clear the mask for bsn_vxlan_network_id making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_VXLAN_NETWORK_ID_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_vxlan_network_id, 0, \
        sizeof(((_match)->masks).bsn_vxlan_network_id))

/**
 * Test whether the match is exact for bsn_vxlan_network_id
 */
#define OF_MATCH_MASK_BSN_VXLAN_NETWORK_ID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_vxlan_network_id))

/**
 * Test whether key bsn_vxlan_network_id is being checked in the match
 */
#define OF_MATCH_MASK_BSN_VXLAN_NETWORK_ID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_vxlan_network_id))


/* Mask/value check/set macros for ofdpa_mpls_type */

/**
 * Set the mask for an exact match of ofdpa_mpls_type
 */
#define OF_MATCH_MASK_OFDPA_MPLS_TYPE_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.ofdpa_mpls_type, 0xff, \
        sizeof(((_match)->masks).ofdpa_mpls_type))

/**
 * Clear the mask for ofdpa_mpls_type making that field inactive for the match
 */
#define OF_MATCH_MASK_OFDPA_MPLS_TYPE_CLEAR(_match) \
    MEMSET(&(_match)->masks.ofdpa_mpls_type, 0, \
        sizeof(((_match)->masks).ofdpa_mpls_type))

/**
 * Test whether the match is exact for ofdpa_mpls_type
 */
#define OF_MATCH_MASK_OFDPA_MPLS_TYPE_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).ofdpa_mpls_type))

/**
 * Test whether key ofdpa_mpls_type is being checked in the match
 */
#define OF_MATCH_MASK_OFDPA_MPLS_TYPE_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).ofdpa_mpls_type))


/* Mask/value check/set macros for bsn_l3_dst_class_id */

/**
 * Set the mask for an exact match of bsn_l3_dst_class_id
 */
#define OF_MATCH_MASK_BSN_L3_DST_CLASS_ID_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_l3_dst_class_id, 0xff, \
        sizeof(((_match)->masks).bsn_l3_dst_class_id))

/**
 * Clear the mask for bsn_l3_dst_class_id making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_L3_DST_CLASS_ID_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_l3_dst_class_id, 0, \
        sizeof(((_match)->masks).bsn_l3_dst_class_id))

/**
 * Test whether the match is exact for bsn_l3_dst_class_id
 */
#define OF_MATCH_MASK_BSN_L3_DST_CLASS_ID_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_l3_dst_class_id))

/**
 * Test whether key bsn_l3_dst_class_id is being checked in the match
 */
#define OF_MATCH_MASK_BSN_L3_DST_CLASS_ID_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_l3_dst_class_id))


/* Mask/value check/set macros for nsh_np */

/**
 * Set the mask for an exact match of nsh_np
 */
#define OF_MATCH_MASK_NSH_NP_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.nsh_np, 0xff, \
        sizeof(((_match)->masks).nsh_np))

/**
 * Clear the mask for nsh_np making that field inactive for the match
 */
#define OF_MATCH_MASK_NSH_NP_CLEAR(_match) \
    MEMSET(&(_match)->masks.nsh_np, 0, \
        sizeof(((_match)->masks).nsh_np))

/**
 * Test whether the match is exact for nsh_np
 */
#define OF_MATCH_MASK_NSH_NP_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).nsh_np))

/**
 * Test whether key nsh_np is being checked in the match
 */
#define OF_MATCH_MASK_NSH_NP_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).nsh_np))


/* Mask/value check/set macros for bsn_tcp_flags */

/**
 * Set the mask for an exact match of bsn_tcp_flags
 */
#define OF_MATCH_MASK_BSN_TCP_FLAGS_EXACT_SET(_match)   \
    MEMSET(&(_match)->masks.bsn_tcp_flags, 0xff, \
        sizeof(((_match)->masks).bsn_tcp_flags))

/**
 * Clear the mask for bsn_tcp_flags making that field inactive for the match
 */
#define OF_MATCH_MASK_BSN_TCP_FLAGS_CLEAR(_match) \
    MEMSET(&(_match)->masks.bsn_tcp_flags, 0, \
        sizeof(((_match)->masks).bsn_tcp_flags))

/**
 * Test whether the match is exact for bsn_tcp_flags
 */
#define OF_MATCH_MASK_BSN_TCP_FLAGS_EXACT_TEST(_match) \
    OF_VARIABLE_IS_ALL_ONES(&(((_match)->masks).bsn_tcp_flags))

/**
 * Test whether key bsn_tcp_flags is being checked in the match
 */
#define OF_MATCH_MASK_BSN_TCP_FLAGS_ACTIVE_TEST(_match) \
    OF_VARIABLE_IS_NON_ZERO(&(((_match)->masks).bsn_tcp_flags))


#endif /* Base header file */
